# ferock 的个�? todo

> 想法池（草稿区），约定见根目�? [todo.md](../todo.md)。决定动手的条目转仓�? Issue 后从这里删除�?

## vbman 编译一致性（C3 vs 微软 VB6�?

- [ ] 对照微软�? VB6，让 vbman 代码的编译结果在 C3 下与 VB6 编译结果一致，包括 VBMAN.dll 的对应执行结�?
  - 基线：VB6Mini 编译 `tests\vbman\src\VBMAN.vbp` 成功�?129 模块，产�? dist\DLL\VBMAN.dll 2.3MB）；C3 前端 129 模块 0 error，cl 阶段 21 错误（详�? [docs/vbman/001_first_compile.md](../docs/vbman/001_first_compile.md)�?
  - 修复顺序：跨类调用实参缺�? ×15 �? 事件 sink 声明 ×3 �? Variant 转换散点 ×3，每修一类跑 `docs\vbman\build_vbman.ps1` 观察错误数递减
  - 编译一致后进入运行一致：VB6 �? C3 各自产出 VBMAN.dll，对比执行结�?
  - 待用户后续提�? vbman 单元测试

## changelog

### 0.10.7

- 2026-09-23��tests/run_tests.ps1����**�� Windows PowerShell 5.1 �»ع�ȫ��** �� Get-MsvcToolset �� Join-Path ����λ�ò���д����$incDir $sdkVer um���� PS 5.1 ��ֻ����ǰ 2 ��λ�ò������������״����ӱ���ʽ��Ĭ��� -> INCLUDE/LIB ������ Windows SDK ·����ȫ�� -> cl.exe �� C1083 �޷��򿪰����ļ� stddef.h/windows.h���ع� 100% FAIL (compile)����Ϊ��ʽ "$incDir\$sdkVer\um" ƴ�ӣ�PS 5.1 �� pwsh 7 ��Ϊһ�£���GitHub Actions �� shell: pwsh�����ܴ�ȱ��Ӱ�죬�ʽ�Ӱ�챾�� PS 5.1 ������

- 2026-09-22��.github/workflows/ci_vbman.yml����**VBMAN ��·������ x86 Ŀ��**����VBMAN �� 32 λ VB6 Դ���ʲ���Long ��ָ���� x64 �±��ضϣ���x64 ��·ԭ�����ܲ�ͨ��5 �� MSVC ����ע����� 3 ���� vcvarsall x86������ VBMAN��׮�����رࡢ���� demo����2 �����Ᵽ�� x64���Խ� C3.exe ������׮�����е��ؽ� ���� C3.exe �ǹ������ߣ����ܱ���Ŀ��λ���޹أ���3 �� C3 ���ü� --arch x86��ע����ע������ SysWOW64 �µ� 32 λע������PE У��� 0x014C��ProgId ��Ч�԰� HKCR\WOW6432Node ��ͼУ�顣����·�� dist\DLL �� output\demo ���䣬artifact ��Լ����Ӱ�졣
- 2026-09-21��src/backend/stmt/cgen_jumps.cpp����**Fix 181��Exit Property װ�ط���ֵ**����visit(ExitStmt) �� ExitKind::Property ��֧�㷢�� return;, ����׼ VB6 ����ģ�壨On Error GoTo EH ... Exit Property ... EH: PrintError���������˳�·������, ����β��ʽ return vb6_ret_<name> ���� EH ��ǩ֮�󡣴�����ֵ�� Property Get ��·�����ؼĴ�������ֵ: x86 �� cTlsReMaster_prop_get_pvSocket �� EAX ǡ�ò��� me, ���õ�� cTlsReMaster ʵ���� cTlsSocket ���� Bind, �� m_oSocket(x86ƫ��8) 0xC0000005������ x86 ����ʵ֤, RTL/����C ˫̽��+��������ض�λ����x64 ͬ��, ������� RAX ǡ�ɶ���δ���Ρ��޷�: currentReturnVar_ �ǿ�ʱ�� return vb6_ret_<name>; (Property Let/Set ������ return)���޸��� x86 ��· COM ȫͨ: cHttpServer ����/Router/Reg/Create/Bind ����ȫ�� hr=0��
- 2026-09-21（src/backend/detail/expr/cgen_expr_member_class_module.inc）：**Fix 180：成员调用的接收者按值上下文发射**——IndexOrCallExpr/CallStmt 置 asCallCallee_=true 后 emit MemberAccessExpr, 该标志泄漏到接收者求值; 接收者是 PropertyGet（cTlsRemaster 内 pvSocket.Bind(...) 的 pvSocket）时 IdentifierExpr 只发裸函数名（本意 callee 由外层补参）, 外层却把裸名当对象表达式前置 → vb6_cTlsSocket_Bind(prop_get_pvSocket,...) 把函数指针当实例传, me->m_oSocket(偏移0x10) 读到 SendText 代码字节(0x8948C4334800227C, 非规范地址), 晚绑定 getDispid 解 vtable 0xC0000005（VBMAN CI 实证）。修法：优先级2 类实例成员访问分支 emitExpr(*node.object) 前后 save/restore asCallCallee_（强制 false）, 接收者按值上下文生成 prop_get_pvSocket((void*)me)。本机全量回归受 reg.exe 策略限制, 待 CI github-test-011。
- 2026-09-21��src/backend/detail/stmt/cgen_setlet_set_rhs.inc��src/backend/detail/util/cgen_state.inc��src/driver/detail/driver_codegen_voidfield_scan.inc����**Fix 179��Set <Object> = New <������> ��������� IDispatch ��װ��**����void* Ŀ��ı�ʾ�� IDispatch*��vb6_ComCall �� lpVtbl / vb6_ReleaseObject ֱ�� Release���������� vb6_ComPack_<X>������ VARIANT*������ COM ʵ�Σ���179b����װֵ�ٸ� typed ������Ŀ��ʱ�� vb6_ComObject_GetInstance �����voidfield ɨ�貹 Object��void*��As Object �ֶδ�ǰ©�� void ����179a Ŀ���ж�ʧЧ������ VBMAN cTlsSocket Set m_oSocket = New cAsyncSocket �� Bind �� ComCall 0xC0000005��Fix 178 �� stack ǰ���ĵڶ�����

- 2026-09-21��src/backend/decl/cgen_decl_var.cpp����**Fix 178��WithEvents �������ֶα�����󶨣����ٱ� Fix 056a ǿת COM ����** �� cHttpServer �� Private WithEvents m_oServer As cTlsReMaster �����н� knownObjectVars_��With m_oServer ���� .Protocol/.Bind ���� vb6_ComSetProp/vb6_ComCall(��ṹ��) �� vb6_getDispid ������ __comObj �� ������ 0xC0000005��VBMAN_DEMO Form_Load��2026-09-21 CI ����ʵ֤�������� VBMAN.dll+0x1AC151/+0x28F1B1 ���Ż�ȷ�ϣ����޷���056a ��֧�ӹ�����������lookupModuleDotted ���� SymbolKind::Class �ҷǽӿ�������ǿת����δ�������ͣ�VBControlExtender �ȣ���Ϊ���䡣

- 2026-09-21��.github/workflows/ci_vbman.yml����VBMAN/demo ���벽�� -g��/Zi /DEBUG /MAP���Ż��� /Od ���䣩��VBMAN.map ���� vbman-dll artifact ���� ��һ�� CI ����ʱ map �� DLL ͬԴͬ���֣�VBMAN.dll+0x1AC151 �ȱ���ƫ�ƿ�ֱ�ӷ��Ż�����������

- 2026-09-21��.github/workflows/ci_vbman.yml�������������ע�ͣ��� -35 �У���ɾ��ʷʵ֤/�ִ����£���������ǰ��Ч�Ŀӵ�Լ����pwsh ��װ�Զ� exit ���Ž���̽�� CRLF ��һ��System32 regsvr32 + Start-Process -Wait���ྻע�������������߼���Ķ���PyYAML У��ͨ�����ṹ���䣨vbman 10 �� / demo 9 ������

- 2026-09-21��src/driver/driver_crossmod.cpp��src/semantics/symbol_table.{hpp,cpp}��src/backend/cgen_util_com.cpp��src/backend/detail/util/cgen_helpers.inc��src/backend/expr/cgen_expr.cpp��src/backend/detail/expr/cgen_expr_ident_symbol.inc��cgen_expr_ident_dispatch.inc����**Fix 177b���������ڱ� coclass �ġ��������滻�����ǡ������滻��** ���� ��һ�棨b06e1f7���� builtin ComClass ��ģ���������Ƴ����ù��������ռλ��������� coclass ����ͨ·��VARIANT ��ʾ / comDefaultMemberName / ComCall ��� / ����ֵ��������������д�ɹ�������󶨴��룬VBMAN 87 �� cl �������C2063 δ�����ʶ�� `Dic(...)` / C2440 `vb6_cls_Dictionary*` ? `vb6_VARIANT` ��ת / C2106�������ױ��� heal �ر�ͬ��ʧ�ܡ�������˷����Ƴ���eraseModuleSymbol ɾ��������Ϊ����ģ��ע��ʱ�� builtin ComClass �ϼ� `Symbol::comProjectImplClass = <��������>`�����ű�����`As Dictionary` ���� coclass ���󶨣����ɴ��������������仯�������� `CCodeGen::comNewExprFor(sym)`�������㣨`visit(NewExpr)` ComClass ��֧ + 4 �� Dim As New �Զ�ʵ�����������ڱα��ʱ�ķ��� `vb6_ComPack_<��>(vb6_cls_<��>_New())`�������� ExeComBridge 03 �� IDispatch ��װͨ·������ void*(�� VARIANT* VT_DISPATCH) �� `vb6_NewObject` ��ʾһ�£������ڶ��󼴹�����ʵ�������г�Ա `Count()/Exists()` ����װ�� GetIDsOfNames �����ַ���b06e1f7 �� `vb6_HresultToErrNum` �����ӳ�䱣�������桤ȱ��Ա������ȷ���壩����֤������ emit-c + cl ���룬�������ڣ���VBMAN.vbp ���� exit=0����ǰ 87 ���󣩡��� c3-error.log������ VBMAN.dll�����ɴ��� `vb6_NewObject(L"Scripting.Dictionary")` ���� 0���������Ѽ� `vb6_ComPack_Dictionary((vb6_cls_Dictionary_New()))`���޶��� `New Scripting.FileSystemObject` �Ȳ���Ӱ�졣

- 2026-09-21��src/driver/driver_crossmod.cpp��src/semantics/symbol_table.hpp��src/semantics/symbol_table.cpp��src/rtl/core/vb6com/vb6com.c����**�ޡ��������౻�������Ϳ�ͬ�� coclass �ڱΡ����µ� VBMAN ��·����ʧ��** ���� VBMAN.vbp ������ `Class=Dictionary` ������ Microsoft Scripting Runtime���� `Scripting.Dictionary`�������Ϳ������ driver_semantics �ڸ�ģ�� analyze ֮ǰ�� builtin ComClass ע��ģ�������򣬿�ģ��ע��׶� `lookupModule` ��������ֱ�� continue�������� `Dictionary` ��Զ������ģ�������� �� 89 �� `As Dictionary` ȫ������ COM ����`vb6_NewObject("Scripting.Dictionary")` + ���󶨵��ã��� ����������չ API��`Count()` / `Exists(Key, CompareCase)`��ʱ�õ� `DISP_E_MEMBERNOTFOUND`(0x80020003)����� 16 λ = 3 �� ����Ϊ VB6 ���� 3��û��GoSubʱ��Return����demo �� Run-time error '3' ��800 �˿ڲ�����ϱ����� GoSub/Return �����޸����㣺�� ��ģ��ע��ʱ�� builtin ComClass/ComInterface ��Ϊ*���ڱ�*������ `SymbolTable::eraseModuleSymbol` ���Ƴ����ڱη��ţ�����֤ VB6 ���塸�����ڶ������������ÿ⡹���� `vb6_ComCheckError` �� `vb6_HresultToErrNum`���� `DISP_E_*` ϵ����ʽӳ�䵽 VB6 �ٷ�����ţ�438/13/449/450/451������������ 16 λ = ����š������� DISP ����ռ�ļ�����֤������ emit-c ��̬��֤���������ڣ���VBMAN.vbp ���ɴ����� `vb6_NewObject(L"Scripting.Dictionary")` �� 23 ���ļ���ʹ���潵Ϊ 0��`vb6_cls_Dictionary_New` ���� 281 ����`Scripting.FileSystemObject` ���޶����÷�����Ӱ�죨15 ������ȫ���ع� `.bas` 152 ͨ�� / 0 ʧ�ܡ�`.vbp` 37 ͨ�� / 2 ʧ�ܣ�Ψ��ʧ��Ϊ���е� Charts 2020/ucTreeMaps `.pag` parser ��֧�����⣩��

- 2026-09-21（tests_github/t2_cases/test_exe_com_bridge/{test_exe_com_bridge.vbp, Module1.bas, bHello.cls}、tests_github/run_t2.ps1�?.github/workflows/ci_vbman.yml�?.github/workflows/ci_t0.yml）：**EXE 工程类过 COM 边界崩溃修复 阶段 4/4：回归用例入�? + VBMAN 链路目录统一�? tests_github** —�? �? 新增 T2 执行断言用例 `test_exe_com_bridge`（EXE 工程 + `MultiUse` 工程�? bHello + `Scripting.Dictionary` 当对端，不依�? VBMAN.dll）：`d.Add "x", New bHello` �? 从对端取�? �? `CallByName o, "Ping", VbMethod` / `CallByName(o, "Echo", VbMethod, "abc")` 往�? �? 断言 `PING-OK` / `EXEB:2/2` / `EXE-COM-BRIDGE PASSED`；清�? 7 �? 8（含 `-ne 8` 门禁与头注释同步）�?**A/B 证据**：修复版编译运行 exit=0�?8 行输出全中；�? `comPackExpr` �? NewExpr 分支临时�? `false` 重编 C3 后，**同一用例�? `EXEB:1 before Add` 后即段错误（exit=139 �? 0xC0000005�?**，还原后重编复跑仍全绿。② `ci_vbman.yml` 全部 `tests/` 引用�? `tests_github/`（实�? 20 处：clone 目标、编译与产物路径、artifact 上传与回读、Form_Load 探针 `$f`、regsvr32 注销路径；vbman �? vbman-demo 保持平级，demo �? `..\..\..\vbman\dist\DLL` 相对引用不受影响），tests\ 方向正式废弃。③ `ci_t0.yml` �? `run_t2.ps1` 的过时注释对齐（vbp 7 �? 8 执行断言、GUI 3 �? 4 只编译；run_t2.ps1 触发来源更正�? ci_t0.yml �? t2 job）�?**本机自检**：清单一致性（8 vbp + 4 GUI 文件全部存在、`-ne 8` 门禁与实参数量一致）；用例源文件统一 CRLF、纯 ASCII�?**�?**：本机跑不了整套 `run_t2.ps1`（沙箱拦 `cmd` �? vcvarsall 采集失败会全红），T2 侧结论以 CI 为准�?

- 2026-09-21（src/backend/detail/base/cgen_base_generate_c_open.inc、src/backend/module/cgen_com.cpp、src/backend/detail/util/cgen_api.inc、src/backend/cgen_util.cpp、src/backend/detail/util/cgen_util_dllentry_exports.inc、src/driver/detail/driver_codegen_dll_entry.inc、src/driver/driver_link.cpp）：**EXE 工程类过 COM 边界崩溃�?0xC0000005）修�? 阶段 1/4：把 IDispatch 生成�? ActiveX DLL 侧解锁到 EXE �?** —�? 根因：C3 �? IDispatch 生成�? 8 �? `isDll_` 守卫锁在 DLL 侧，EXE 工程类实例无 `__comObj` 首字段、无 coclass 表、`vb6comserver*.c` 不链入，`New <工程�?>` 产出�?**�? C 结构体指�?**�? DLL 侧当 IDispatch AddRef （`Set Controllers(...) = Controller`，cHttpServerRouter.cls:63）→ 首字段被�? vtable �? `callq *0x8(%rax)` �? AV。改：`__comObj` 字段无条件生成（并配 vb6comserver.h include 条件放开到所有类模块，防字段访问�? C4013 截断指针）；`_New()` 初始�? `__comObj = NULL`；类字段访问器放开 EXE；`generateDllEntry` 增第三参 `includeDllExports`（导出函数段�? DllMain 包进守卫、`g_vb6_coclassCount` �? if 外）；EXE 平行生成 `com_entry.c`（progId �? projectBaseName_ �? options.dllProgId �? "VB6EXE"）；vb6comserver 5 �? .c 无条件链入（com_entry.c �? `!msvcOpts.isDll` 链入，DLL 不生成该文件）�?**验证**：本机（Windows + `tmp/msvc_env.sh` 替身 MSVC 环境）增量构�? C3.exe exit=0；`VBMAN_DEMO.vbp --emit-c` 生成物含 `__comObj` 字段�? com_entry.c coclass 表；VBMAN.dll 用新 C3 重编�?129 模块）编译链接通过；纯 EXE �? `__comObj` �? NULL 无读方，行为零变化�?**风险**：`__comObj` 成为 EXE 类结构体首字段属全量布局变更，静态论�? + 全量回归确认无影响；EXE 不跑 TypeLib 构建，CLSID/IID 走确定性哈�? fallback�?
- 2026-09-21（src/backend/cgen_util_com.cpp、src/backend/module/cgen_com.cpp、src/backend/detail/base/cgen_base_generate_c_open.inc、src/rtl/core/vb6comserver/vb6comserver.h、src/rtl/core/vb6comserver/vb6comserver_obj.c）：**EXE 工程类过 COM 边界崩溃修复 阶段 3/4：COM 实参打包点接入（真正止血�?** —�? 阶段 1 只让工程类「有」IDispatch，`New bHello` 仍以裸结构体指针�? VARIANT。改：`comPackExpr` 开头加 NewExpr 分支，命中本工程类（`lookupModuleDotted(name)->kind == Class`）时改走按类打包函数 `vb6_ComPack_<�?>`（ComClass / 外部 ProgID 等其余分支零改动，天然收窄）；`emitClassFactory` 末尾为每个类模块发射该函数，`.h` 里紧跟类工厂声明输出声明（调用点常在其他模块，缺声明 = 隐式 int 截断指针）；RTL 新增 `vb6_ComPackVB6InstanceRaw(classVariable, instance)`（FindCoClassDesc �? ComObject_FromInstance �? VARIANT* VT_DISPATCH）�?**引用计数**：FromInstance 给出�? 1 个引用直接转移给 VARIANT，不额外 AddRef（否则包装器与实例永不释放）；类未进 coclass 表（Private / �? MultiUse|SingleUse）返�? NULL，等�? Nothing 而不冒充对象�?**有意不动**：`visit(NewExpr)` 的输出（�? `vb6_cls_X_New()` 留给本工程内部早绑定）、`inferExprType` �? NewExpr case、FireEvent 广播（EXE 无此需求）�?**验证（本�? A/B 对照�?**：本机无法复�? CI �? demo 场景（该 demo 去掉 Reg、只�? `.Start 800` 同样 AV，旧/�? DLL 皆然，属与本次无关的既有问题），改用独立最小工�? `tmp/verify_p3/`（EXE + MultiUse 工程�? + Scripting.Dictionary 当对端，不依�? VBMAN.dll）：修后 `d.Add "x", New bHello` �? 取回 �? `CallByName o, "Ping", VbMethod` 输出 `S1..DONE / PING-OK / S5 echo=echo:abc`、exit=0、无弹框�?**反证**：临时把该分支置 false 重编 C3 �? 同一工程 `S1 before Add` �? exit=3221225477 (0xC0000005)，与 HANDOFF 记录现场一致；恢复后重编再跑仍全绿。全�? `--emit-c` 回归 **151 �? .bas 全通过**；VBMAN.dll 用当�? C3 重编�?129 模块）编译链接通过�?**已知限制**：`Set h = New bHello`（赋�? `As Object` 变量）后�?**变量**�? AV（`tmp/verify_p3b/` 复现）—�? comPackExpr 只识别字�? `New` 表达式，变量路径走通用 `vb6_ComPackObject`；VBMAN_DEMO 用的是字�? New，不影响止血目标�?

- 2026-09-20（src/driver/rtl_embedded.cpp、src/driver/rtl_embedded.hpp、src/driver/driver_link.cpp）：**�? T1 回归 `test_variant_x86` 编译�? C1083 的会话目录清理竞�?** —�? 现象（`github-test-011` run 35517635219）：T0 绿、T1 纀��T2 �? T1 失败�? skip，整轮回归无法通过；T1 `PASS=73 FAIL=1 TOTAL=74`，唯一失败�? `test_variant_x86: compile FAIL`�?12 个源文件�? `c1: fatal error C1083: Cannot open source file '...\Temp\C3C\2071056695000_5784\rtl/ax_site.c'`（ax_site / ax_load / ax_propbag / ax_host / uc_host / uc_host_window / uc_hostmodel / uc_controls / uc_collection / uc_debug / uc_propbag / vb6forms_axcontainer）。原因：`cleanupOldSessions()` 在每�? `create()` 里跑，拿**目录名里�? steady_clock 计数**判过期、阈�? 300 秒；�? T1 �? x64+x86 双架�? 16 路并发，慢用例（demo 链路单轮 cl /MP 要编 100+ �? RTL 源文件）实测会超�? 300 �? �? 兄弟进程�?**仍在编译**的会话目录整棵判过期删掉，`cl /MP` 里「尚未读到」的那批源文件就�? C1083（已编译的不受影响，所以只缺一个连续子集）。同一 tag 10:19�?35504738978，加 PID 后缀那次）还 success�?14:48 �? failure，也说明是并发时序而非代码逻辑。修两道防线：① **治本**：`cleanupOldSessions()` 不再解析目录名，改用「目录名后缀 PID 的属主进程是否存活」（`OpenProcess(PROCESS_QUERY_LIMITED_INFORMATION)` + `GetExitCodeProcess`，`ERROR_ACCESS_DENIED` 保守当存活）+ 目录自身 `last_write_time` 的文件系统时钟，阈值放宽到 30 分钟；属主活着就绝不删，阈值只兜住崩溃/被杀的残留；�? **兜底**：新�? `SessionManager::restoreMissing()`，编译前逐条核对 RTL 清单、`file_size==0`（或目录被删）的当场�? RCDATA 资源重新解包，`driver_link.cpp` �? `compileAndLink()` 之前调用（`runLinker` 是全仓唯一编译入口）。顺带把 `files[]` 清单�? `create()` 内提升为文件作用�? `kRtlFiles[]` + `kRtlFileCount`，与 `restoreMissing()` 共用，避免两处清单漂移�?**验证**：增量构�? `c3` exit=0（`[5/5] Linking CXX executable C3.exe`）；清理守卫单测 —�? `x_4`（属主存�? pid=4 + 目录陈旧）保留、`y_999997`（属主已�? + 陈旧）删除、`z_999996`（属主已�? + 刚写）保留；自愈端到�? —�? �? 129 模块�? `VBMAN.vbp` 在编译中途把 5 �? RTL 源文�?**截断�? 0 字节**，日志出�? `C3: warning: restored 5 missing RTL file(s) in ...\133193435922000_32196\rtl`，随�? `VBMAN.dll`�?2694656 字节）正常产出，证明自愈在真实慢工程上生效�?**跑验证的两个�?**：本机沙箱对批量删除有安全门（`SAFE_DELETE_BULK_CONFIRM_REQUIRED`），`rm` �? `os.remove` 都会被静默拦掉（两轮「删 5 文件」验证都不作数，改用「截断为 0 字节」绕�? —�? `restoreMissing` 的判据正好是 size==0）；更要紧的�?**别把 `TMP` 指到工作区内**：C3 退出时清理会话目录要批量删 100+ 个文件，同样撞上这道门被挂起，表现为 C3.exe 长时间不退出（实测 13 分钟、目录里残留 13 �? .obj 与空 `rtl/`），看着像卡死实际是被安全门挡住
- 2026-09-20�?.github/workflows/ci_vbman.yml）：**demo 运行步加崩溃现场观测** —�? 补齐 `Erl` �? demo 首次真正跑起来，失败点从「编译失败（LNK2019）」前移到「运行期崩溃」：`demo exited early (code -1073741819)` = `0xC0000005` 访问违例，启动约 2 秒内即死、无 VB6 错误弹窗、无 429 弹框�?800 端口未监听。访问违�?**不会�? VB6 �? `On Error` 捕获**，所以探针的 `_formload_err.txt` 也写不出来（该分支根本没进）。改为：运行前设 `$env:C3_CRASH_TRACE = "1"`（`vb6forms.c` Fix 149 的未处理异常过滤器，�? `vb6_ShowForm` 安装，正好覆盖「延迟派�? Form_Load」那段），崩溃时生成�? exe 会把「异常码 + 触发模块 + 48 帧模块偏移」写�? CWD �? `c3_crash.txt`；并�? `finally` 里回显该文件（产物路�? `output/demo/**` 已覆盖，�? artifact 一起带走）。注：C3 链接未产�? `.map`/`.pdb`，帧只能到「模�? + 偏移」，够区分崩溃发生在 `VBMAN_DEMO.exe` 还是 `VBMAN.dll`�?
- 2026-09-20（src/rtl/core/vb6rtl/vb6rtl.c、vb6rtl_file.c、vb6rtl_financial.c、vb6rtl_string.c）：**补齐 7 个「只有声明没有定义」的 RTL 内建函数** —�? 全仓扫描（剥掉注释与字符串后做「声�? �? 定义」配对）查出 730 条声明里�? 7 条无定义、且�? 7 条全部会�? codegen 发射：`vb6_Erl`（cgen_expr_ident_builtin.inc 映射 `erl`）、`vb6_Tab`(P21-28)、`vb6_Spc`(P21-29)、`vb6_FileAttr`(P21-17)、`vb6_NPer`(P21-16)、`vb6_IRR`(P21-19)、`vb6_MIRR`(P21-20)，声明集中在 vb6rtl_class_com.h:28~51。用到它们的工程一律卡在链接期 `LNK2019: unresolved external symbol`（CI vbman demo 实证：探针用 `Erl` �? `Form1.obj : error LNK2019: unresolved external symbol vb6_Erl` �? `fatal error LNK1120`）。实现落点按家族就近：`vb6_Erl` �? vb6rtl.c �? Err 三兄弟旁（行号嵌入机制未实现，按 VB6「源码不带行号时 Erl 返回 0」语义返�? 0）；`vb6_Tab`/`vb6_Spc` �? vb6rtl_string.c（复用既�? `vb6_Space`）；`vb6_FileAttr` �? vb6rtl_file.c（须访问 `static` �? `vb6_file_table`/`vb6_file_mode`：attribute=1 返打开模式�?=2 �? `_fileno` 句柄，通道未打开�? 52）；`vb6_NPer`/`vb6_IRR`/`vb6_MIRR` �? vb6rtl_financial.c，与 FV/PV/RATE/NPV 同风格，数组取值抽成文件内 `vb6_fin_elem`（元素分派与 vb6_NPV 逐分支一致，�? double/single/long/int/byte/variant）�?**验证**：全仓复�? 730 条声�? 0 条缺定义；本机增量构�? C3.exe exit=0；实现签名与头文件声明逐条对应；真实工程旁�? —�? vbman 源码 8 处引�? `Erl`、ZipArchive demo 引用 `FileAttr`，并非空雷�?**�?**：本机沙箱拦 `cmd`/`reg.exe` 导致 vcvarsall 环境采集失败（回归全红是环境假象），本轮回归改交 GitHub Actions 跑（`ci.yml` 全量回归，workflow_dispatch 触发）�?
- 2026-09-20�?.github/workflows/ci_vbman.yml）：**�? VBMAN 链路 demo job 探针步骤误报「Form_Load 块未匹配�?** —�? 探针把上�? Form_Load 原文写进单引�? here-string 做逐字符匹配，�? GitHub runner 落地的临�? .ps1 �? CRLF 行尾、here-string 会原样保�? CR，`$src` 却已�? `.Replace("`r`n", "`n")` 归一�? LF �? `.Contains($old)` �? false（github-vbman-011 实证，报错正文与本次一致）。本�? A/B 复现台（探针脚本 LF/CRLF × 上游 .frm LF/CRLF 四组合）给出判据：脚�? LF 时四组合�? matched=True（含 CRLF 上游），脚本 CRLF 时两组合�? matched=False 并复现同一 throw，`$old` �? CR �? 9 �? `$src` �? 1。修：here-string 之后�? `$old`/`$new` 各自�? `\r\n �? \n` 归一（与 `$src` 同口径），并�? throw 正文带上 `oldLen`/`srcLen` 便下次自证�?**验证**：四组合�? matched=True + injected=OK，注入产�? Form_Load �? `On Error GoTo FLEH` �? `_formload_err.txt` 落盘逻辑；PyYAML 解析�? workflow 通过、语句顺序校验通过。附：`gh run watch` 直连 api.github.com �? TLS handshake timeout 中断，重试即�?
- 2026-09-20（src/driver/detail/driver_codegen_dll_typelib.inc、src/rtl/core/vb6comserver/vb6comserver.c、src/typelib/typelib_builder.hpp）：**�? VBMAN 链路 CI 的运行期 429�?0x800401F3�?** —�? C3 生成 ActiveX DLL 时把类型库内部名写成 `<ProgID>.TypeLib`，而真 VB6 用的是工程名（实�? vbman 仓库里预编译�? VB6 �? VBMAN.dll：库�? `VBMANLIB` 紧跟�? LIBID `{A638B373-E3B1-4281-8BD3-957EAD54B31C}` 之后，全文件 ASCII �? UTF-16 均无 `.TypeLib` 命名）。客户端导入该类型库时，coclass �? ProgID 靠注册表反查 `ProgIDFromCLSID`，`vb6_RegisterCoClass` 只写�? `HKCR\CLSID\{clsid}` 默认值和 `HKCR\<ProgID>`、没�? `ProgID` �? �? 反查必失败，退化成「类型库�? + 类名」拼�? `VBMANLIB.TypeLib.cHttpServer` �? demo 运行�? `CLSIDFromProgID` 查不�? �? New �? 429 弹窗卡住 Form_Load�?800 端口不监�? �? HTTP 断言失败。修：①类型库内部名改回工程名（LIBID 种子不动，免给既有产物换 GUID）；②注册时补写 `HKCR\CLSID\{clsid}\ProgID`�?**验证**：本机最小工程复现同一失败形态（自建 `TINYLIB.dll` + 客户端模块级 `Private mFoo As New cFoo`）——修�? tlb 内名 `TINYLIB.TypeLib`、生�? `vb6_NewObject(L"TINYLIB.TypeLib.cFoo")`，修�? `TINYLIB` / `L"TINYLIB.cFoo"`；编译类 10/10 全过，另�? `test_activex_dll.vbp`、`test_event_dll.vbp`、`diff_smoke.vbp` exit=0。附：本机经 PowerShell �? `run_tests.ps1` 会全红（沙箱�? `cmd` �? MSVC 环境采集失败 �? `C1083 stddef.h`），同样用例�? Bash 直调 C3 全过，属环境假象
- 2026-09-20（src/rtl/core/vb6forms/vb6forms_axsite.c �? axsite/ 5 �? .c + vb6forms_axsite_internal.h、src/driver/{c3rtl.rc,rtl_embedded.hpp,rtl_embedded.cpp,driver_link.cpp}、CMakeLists.txt）：vb6forms_axsite.c�?1029 行）按功能家族纯搬移拆为 axsite/ �? 5 个编译单元（最�? 363 行），主文件降为 32 行伞文件；顺带补登记 vb6forms_axcontainer.c（CMake 漏项）。验证只到静态复�? + 四处清单 105/105/105/105�?**CI 首轮实测�? `ax_host.c` C2065 'bag' undeclared �? C1003** —�? 原因是内部头只搬了结构体、没搬结构体所依赖�? include 序言（OLE 接口 vtable 类型�? `<olectl.h>` 提供，`vb6forms.h` 只带 `<windows.h>`），已补全并加「头依赖覆盖」机械化检�?
- 2026-09-20（删�? ai/022-源码拆分进度�?.md；同�? ai/003-开发计�?.md、README.md、src/rtl/core/vb6rtl/vb6rtl.c 注释、tests/regress_all.ps1 注释）：删除拆分专题台账 ai/022�?364 行）并清理全�? 9 处引用（含外�? CLAUDE.md/README、镜�? c3_github），0 悬空残留
- 2026-09-19（src/rtl/core/vb6forms/vb6forms_uc.c �? vb6forms/uc/ 6 �? .c + uc/detail/ 7 �? .inc、vb6forms_uc_internal.h、src/driver/{c3rtl.rc,rtl_embedded.hpp,rtl_embedded.cpp,driver_link.cpp}、CMakeLists.txt）：**`vb6forms_uc.c`�?1546 行）按族拆为 6 个编译单�? + 7 个函数体片段，用二级子目�? `uc/` + `uc/detail/`（纯搬移零重排）** —�? 主文�? 1546 �? �? 57 行；`uc/` �? `uc_host.c` 256 / `uc_hostmodel.c` 257 / `uc_host_window.c` 164 / `uc_controls.c` 157 / `uc_collection.c` 40 / `uc_debug.c` 25（最�? 257）；`uc/detail/` �? `uc_collection_api.inc` 201 / `uc_host_create.inc` 109 / `uc_debug_composite.inc` 91 / `uc_hostmodel_getprop.inc` 88 / `uc_hostmodel_call.inc` 74 / `uc_debug_dib.inc` 66 / `uc_hostmodel_setprop.inc` 32。新�? `vb6forms_uc_internal.h` 承载三类跨族符号：结构体**完整定义**（各族要解引用字段，只前置声明不够）、跨族状态、跨族函数�?**踩坑 �?**：同�? `static` 提升不完整导致首次端到端 `LNK2019` ×4（`vb6_uc_registerClass` / `vb6_uc_gdiplusInit` / `vb6_uc_push` / `vb6_uc_pop`）—�? 判据是�?**谁调用它**」而非「函数属于哪个族」，凡调用点跨族就必须去 `static`�?**踩坑 �?**：`vb6_UCDib` 原定义切�? `uc_debug.c`，�? `uc_host_window.c` �? WM_PAINT �? `sizeof` 与按成员访问，必须上移完整定义。RTL 5 处管线同步（rc 新增 ID 175~192、头注释 77 �? **93 �?** = 25 .h + 55 .c + 13 .inc；枚举、`files[]`、`driver_link.cpp` �? `sourceFiles` �? `vb6forms_uc.c` 换为 6 �? `uc_*.c`、CMake 93 项），顺�?**补登�?** `vb6rtl_userctl.h`（rc 有�? CMake 漏，�? Fix 105 遗留，四处计数由 92/93 不一致修正为 93/93/93/93）�?**验证**：独立复算全过（片段 + 单元逐行一致，剩余 16 行缺口逐条核实�? banner/分节注释/交界空行等账本产物）；增量构�? 0 错误；`hello.bas`(exe) 输出 `Count: 1..10` + `Sum = 5050`、退出码 0，`test_activex_dll.vbp --dll` 编译通过；清�? `%TEMP%\C3C` 后干净解包目录 **93 文件齐全、拆分相�? 15 项逐字节一致（15/15�?**�?6 �? `uc_*.obj` 全产出；全量回归 **0 �? `REG-C3`**（`regress_all.ps1` 137 用例对照：PASS 50 / C3-EXT 36 / BOTH-FAIL 21 / SKIP-AUX 17 / COMPILE-PASS 12 / RUN-DIFF 1，`BOTH-FAIL` 全为预处�? `#Else`、`RUN-DIFF` �? TestAXDLL 未注册的既有摆动项）�?**跑回归的�?**：`regress_all.ps1` �? `cmd /c call vcvarsall ...` 采集 MSVC 环境，本�? `cmd`/`reg.exe` 被安全策略拦 �? 即使 `C3_VCVARSALL` 指向内容正确的替�? bat 也采集失败（日志�? `INCLUDE=False`），�? SKIP 用例会全�? `C1083 无法打开包括文件 stddef.h`、被误记�? 6 �? `REG-C3`�?**正确跑法是在 PowerShell 里先直接注入 `INCLUDE`/`LIB`/`PATH`/`VCINSTALLDIR` 再调脚本**，并用日志第 1 行的 `INCLUDE=True` 作为该轮结论有效的判据。附记：解包会话目录名是 `steady_clock` 计数，跨进程重启会复用同一数�? �? 旧目录被当新会话直接覆盖�?**验证解包内容前必须删干净 `%TEMP%\C3C`**，否则读到的是陈旧快照（本次曾据此误判为缓存失效，实为测量假象）

### 0.10.6

- 2026-09-19（src/backend/decl/cgen_decl_proc.cpp、src/backend/decl/cgen_decl_prop.cpp）：**Fix 123b 补齐 Sub / Property 两处形参注册�?** —�? Currency/Single/Date 形参同属 C double 组，上一提交（f5e72c3）只改了 `visit(FunctionDecl)`，�? `SubDecl` / `PropertyDecl` 各持一份独立复制的注册链，两处仍只注册 `Double`。于�? vbman cZipArchive �? `frFireProgress`�?**Friend Sub**，形�? Current/Total As Currency）走 Sub 链未注册 �? `inferExprType(Current)` 落符号表查找�?129 模块工程�? `current` 撞到同名符号）→ RaiseEvent 实参打包误走 else �? BSTR 分支，生�? `vb6_BSTR_FromStr(double)` �? cZipArchive.c(2341/2343) C2440「无法从 double 转换�? const wchar_t *」�?**验证**：修复前 x86 �? VBMAN.dll exit=1�?2 错）；修复后 **x86 + x64 双架�? exit=0�?0 error**，产�? PE32 / 2229248 字节 �? PE32+ / 2656256 字节，生成代码改�? `__evt_args[1].vt = VT_R8; __evt_args[1].dblVal = (double)(Current);`（与 Currency→double �? Fix 126 值语义一致）；全量回�? PASS=86 / FAIL=0 / SKIP=1 / TOTAL=87�?
- 2026-09-19（src/backend/detail/expr/cgen_expr_call_pad_conv.inc �? 6 个新 .inc、src/backend/expr/cgen_expr_call.cpp）：**pad_conv.inc�?545 行）按函数体内既有分节注释再�? 6 段（纯搬移零行为改动�?** —�? �? `cgen_expr_call_opt_pad.inc` 81（Optional 补齐 / IsMissing `_has_` / 隐式 me）、`cgen_expr_call_conv_cstr.inc` 87（CStr 家族）、`cgen_expr_call_conv_numeric.inc` 88（CInt/CLng/CDbl/CByte/CSng/CBool）、`cgen_expr_call_conv_output.inc` 109（MsgBox / Format 首参 + CStr 兜底）、`cgen_expr_call_conv_varargs_lenb.inc` 87（CallByName 变参 + LenB 分派）、`cgen_expr_call_builtin_argtype.inc` 114（Fix 108c 内建实参适配）；原文�? `git rm`，伞文件 include 12 �? 17 条�?6 片段**全部自身花括号平�?**（可继续独立搬移），切分脚本对「拼�? == 拆分前逐字节一致」做断言。验证：拆分前后各对 VBMAN.vbp �? `--emit-c`�?**109652 行逐字节一�?**；全量回�? PASS=86 / FAIL=0 / SKIP=1 / TOTAL=87�?
- 2026-09-19（src/driver/driver_compile.cpp、src/driver/main.cpp、src/backend/expr/cgen_expr_binary_util.cpp、src/backend/detail/util/cgen_helpers.inc、tests/test_bstr_concat_scalar.bas、tests/run_tests.ps1）：**�? bugfix 文档 260919 全部四项（B2/A-1/A-2/B3�?+ 新增标量拼接用例** —�? �? B2 退出码：`Driver::compile` 把「已显示帮助」与「无源文件」拆开（help/version �? errorCount=0），`main.cpp` �? `!success && errorCount==0` 第三层守卫同步删除（文档只写了两层，实测 `--version` 仍返 1 才发现这层）；② A-1：`wrapToBSTR` �? `expr.find("vb6_BSTR")` 未锚定子串检查改为新�? `isBstrReturningCall()` 顶层前缀精确判定，`vb6_Len("abc")`/`vb6_Val(...)` 等带字符串实参的标量函数不再被实参里�? `vb6_BSTR_FromStr` 整体误判直通；�? A-2：前缀块兜�? `return expr; // 其他vb6_函数假定为BSTR` 删除，落�? `inferExprType(node)` —�? `Err.Number` 生成 `vb6_CStrLong(vb6_ErrNumber())`（即 2026-09-18 遗留�? 0xC0000005 崩溃项）、`UBound` 同理；④ B3：数值清单按 src/rtl 真实返回类型拆成 CStrDbl（Sqr/Abs/CDbl/CSng/Timer/Rnd/Fix/Int/Val，原�? CStrLong 截断 �? C4244�?/ CStrLong（CLng/CInt/CBool/CByte/Len/LenB/InStr/InStrRev/Sgn/VarType）两组，�? Date 检查同步改顶层函数名精确匹配（�? `find("vb6_Time")` 会把 vb6_Timer 吞进 CStrDate 分支）�?**验证**：A-1/A-2 两轮 `--emit-c` 逐字节对�? 99 个用例均零差异（既有用例未覆盖这些路径）；全量回�? PASS=85 / FAIL=0 / SKIP=1 / TOTAL=86（新�? `test_bstr_concat_scalar.bas` 16 项断言：Err.Number 非零 / 带与不带字符串实�? / 整数与浮点标�? / result.txt �? VB6 原生对照，旧基线 82/0/1/83 已过时）；B2 五项退出码 `--version/-V/-h/--help/无参数` = 0、`--nosuchopt`/无源文件/语法错误 = 1。注意：本机沙箱 reg.exe 被拦导致真实 vcvarsall 拿不�? SDK 路径，改�? portable 替身 bat（`tmp/msvc_portable/vcvars.bat`，arch 感知、含 rc.exe、强�? C3 每次编译重调），`run_tests.ps1` �? `C3_VCVARSALL` 认它
- 2026-09-19（src/backend/expr/cgen_expr_binary_util.cpp、src/backend/detail/expr/cgen_expr_call_pad_conv.inc）：**重落 7ce1f50 丢失的真修复（Fix 100a/100b/100c，取�? 425f934�?+ 新增 Fix 100d + 解冲突标�?** —�? 7ce1f50 实际提交的两个源码文件是回退版（isBstrReturningCall 只有声明无实现、Fix 118j 类型分派被还原成「其他vb6_函数假定为BSTR」直通兜底），且 `!21` 合并�? pad_conv.inc 的未解决冲突标记�?<<<<<<< HEAD / >>>>>>> origin/main）直接提交进 HEAD 挡住构建（本�? main 带标记、origin/main 干净，系本地 reset+cherry-pick 链污染），当时验证用�? exe�?11:34）早于提交（12:06）。本次：�? 100a：wrapToBSTR:37 的未锚定 `find("vb6_BSTR")` 子串检查改�? isBstrReturningCall 顶层前缀清单判定；② 100b：直通兜底删除，落到 inferExprType(node)；③ 100c：数值清单按 RTL 返回类型�? CStrDbl/CStrLong 两组、Date 组改顶层函数名精确匹配（vb6_Timer 不再被find("vb6_Time") 吞进 CStrDate）；�? 100d：A-1 同类第二�? :122 `find("vb6_BSTR_FromStr(")` 同步锚定 —�? 修�?"x=" & Asc("A")」裸�? int32_t当指针段错误（实�? exit 139），现生�? vb6_CStrLong(vb6_Asc(...)) 输出 asc=65；⑤ pad_conv.inc �? HEAD 侧并集版 Fix 121 解掉冲突标记�?**验证**：构建通过；`--emit-c` 100 用例对修复版 oracle 零差异（�? 6 处窗体样式常�? 0x50000000�?0x54000000来自 main 侧窗体改动与本次无关，Fix 100d 影响面快照对�? 0 处变化）；全量回�? PASS=86 / FAIL=0 / SKIP=1 / TOTAL=87（test_bstr_concat_scalar PASS）；MSVC C4047/C4024/C2440/C4244 �? 0；样�? len=3 err=0 sqr=1.4142135623731 val=1.5 asc=65 ub=1 退出码 0
- 2026-09-18（遗留，未修；已定位到代码位置与实测证据）：**字符串拼接里�? `Err.Number` 不生成取字符串转�? �? 0xC0000005** —�? `Debug.Print "p13-misserr=" & Err.Number` 生成 `vb6_BSTR_Concat(vb6_BSTR_FromStr(L"p13-misserr="), vb6_ErrNumber())`，把 `int32_t` �? `BSTR` 指针传入 �? 按地址 5 解引用即崩；同一表达式换�? `e1 = Err.Number` �? `"..." & e1`（Long 变量）则正确生成 `vb6_CStrLong(e1)`，说明拼接路�?**只在能判定出具体类型时才补转�?**，�? `Err.Number` 的类型推断没落到 Long�?**实测证据**：`v = c.Item("nosuchkey")`（键不存在，Collection 正确�? Err 5）后紧跟该拼�? �? 进程 `0xC0000005`；改�? `CStr(e1)` 后同一用例 14/14 全对、正常退出�?**排查手法**：在 RTL 临时�? `fwprintf(stderr, ...)`，确�? `vb6_ComCall` 已正常返�? NULL，把崩溃范围压到下一行拼接上。修复方向：让语义层�? `Err.Number`/`Err.Description` 等内建属性推断为具体类型，或在拼接路径对推断不出具体类型的操作数统一�? `vb6_CStr` 兜底
- 2026-09-18（合�? + src/backend/detail/expr/cgen_expr_call_pad_conv.inc、src/rtl/core/vb6forms/vb6forms_uc.c、src/rtl/core/vb6forms/vb6forms_controls.h、src/rtl/core/vb6com/vb6com_foreach.c）：**�? main 并进 `ferock/0.10.6`，并修合并后定向验证暴露�? 3 处缺�?** —�? 合并分两轮：先合 `f57b574`（冲�? 6 处，按「main 优先 + 保留 0.10.6 独有增量」解决；RTL 内建 Collection 资源顺移让位�? main �? UserControl 资源，改�? 177/178，rc 头注释同步修正为 **24 .h + 49 .c + 6 .inc = 79**；`IndexOrCallExpr::omittedArgs` 是两侧同名同义的**真重�?**，按 main 的槽位保留表示落地，并清理被 main 取代�? 0.10.6 �? `LiteralKind::Missing` 死代码），再补合 `aa16121`（VB6 手册文档�?+ `65243ff`�?!18 merge vi/dev）—�? 后�? 480 文件 / 14967 行全�? `docs/vb6-manual/` 下，�? 0.10.6 侧改�?**零文件重�?**故无冲突。产�? `965e02f` + `86b6058`，已�? `origin/ferock/0.10.6`（`ls-remote` 核验 86b6058）。合并后验证查出并修 3 处：�? **`CStr(�? vb6_ComCall/ComCallByDispid/ComGetProp)` 少一层解�?** —�? 这三个函数返回类型是 `void*`、实际指�? Windows `VARIANT`，直接过 `_Generic vb6_VariantFromValue` 会落�? `default:` 被当 VT_DISPATCH 对象指针 �? 实测 `Debug.Print "B=[" & CStr(c.Item(1)) & "]"` �? `B=[]`，而同值走赋值路径（`s = c.Item(1)`）正常，说明只有内联拼接这一路漏�? `vb6_VariantFromComResult`；② **`vb6_Collection_EnumInit/EnumNext` 无原型声�?** �? `vb6com_foreach.c` 隐式按返�? `int` 处理，x64 下枚举句柄指针被截断；③ **宿主枚举句柄被当 `vb6_ForEachState` 解引�?** —�? `vb6_ForEach_Release` 无条件取 `state->pEnum` �? `lpVtbl->Release`，而宿主句柄（Controls 集合 / 内建 Collection）首元素是集合指针不�? `IEnumVARIANT*` �? 0xC0000005，改为先�? `vb6_UC_ControlsIsCollection` / `vb6_Collection_IsCollection` 判定、宿主句柄直�? `free`；同时把两个宿主枚举器句柄首元素�? `int32_t` 改为 `intptr_t` 存指针（`Controls` 枚举器有完全相同的截断缺陷，一并修）�?**验证**：三组探针（省略实参位置 `c.Add "x", , , 1` 不错�? / For Each over Collection / 内联拼接 `CStr(c.Item(1))`）全部通过且退出码 0；全量对照回�? 122 用例与合并前基线 `report-20260918-130528.csv` 逐条比对�?**判定变化�? 1 �?** —�? `test_modulemethod.vbp` RUN-TIMEOUT→PASS（人工点掉真 VB6 侧模态框所致，见下条），其�? 121 例逐条一致；本轮新增 5 例（VbQRCodegen / BalloonTooltips）系 main 带入的测试用例，不是回归
- 2026-09-18（诊断，未改代码）：**�? VB6 侧的致命错误模态框会阻塞对照回�?** —�? 实测 `test_onerror.bas`（真 VB6 版必弹「Run-time error '53' File not found」）�? `test_modulemethod.vbp`（必弹�?424 Object required」）都会�? `regress_all.ps1` 的单例超时记�? `RUN-TIMEOUT`�?**框一被人点掉就变 PASS** —�? 这正是历史报告里同一用例时�? TIMEOUT 时�? PASS 的原因，与代码无关。两例的共同触发条件�?**错误处理器直接掉�?**（结尾没�? `Resume`/`Exit Sub`）：最小用例逐层隔离证实，单�? `On Error GoTo`、单�? `On Error Resume Next`、两个连续失�? `Open` 全程 `Resume Next` **都不�?**，只有「处理器结尾直接落到后续语句」才会让其后的运行期错误变成致命框；�? VB6 文档要求处理器前�? `Exit Sub`，C3 这点�? VB6 宽松故不弹。`test_modulemethod` �? 424 另有一层：用例调用 `MathUtils.Add(10, 20)`（标准模块的 `模块�?.方法()` 限定调用）是 **C3 扩展语法**，真 VB6 在没�? `Option Explicit` 时把 `MathUtils` 当隐�? Variant，运行期�? 424；C3 版正确输�? `30` / `21`�?**顺带记两条口径缺�?**：`regress_all.ps1` �? `C3-EXT` 判定只覆盖真 VB6 **编译�?**失败，这类「VB6 编译期放行、运行期才炸」的 C3 扩展语法会被误记�? `RUN-DIFF`/`RUN-TIMEOUT`；另外回归跑 `.bas` 用例�? cwd 就在 `tests/`，会�? `tests/m6_test.txt`、`tests/scores.txt` 两个受版本控制的运行期输出弄脏（每次跑完需 `git checkout --` 还原，建议后续加�? `.gitignore`�?
- 2026-09-18（src/ast/detail/ast_enums.hpp、src/ast/detail/ast_expr.hpp、src/ast/ast.cpp、src/semantics/semantic_analyzer_expr.cpp、src/parser/parser_expr_postfix.cpp、src/parser/stmt/parser_stmt_assign.cpp、src/backend/expr/cgen_expr.cpp、src/backend/detail/expr/cgen_expr_call_com_bind.inc、src/rtl/core/vb6com/vb6com.h、src/rtl/core/vb6com/vb6com_pack.c、src/rtl/core/vb6rtl/vb6rtl_class_com.h）：**�? COM 调用省略实参导致后续实参前移** —�? `c.Add "x", , , 1`（After=1）在无括号路径被解析�? 2 个实参（中间�? Key/Before 位置直接丢弃），`1` 落到�? 2 个形�? Key �? �? 集合/字典�? Before/After 插入整体错位；实测表现为 `Collection.Add` 抛运行期错误 5（无效的过程调用或参数）。修复分四层：AST �? `LiteralKind::Missing` �? `IndexOrCallExpr::omittedArgs`；parser 无括号路径在 `canStartArg()` 失败时只登记形参位置（不往 positional 塞假值，普通函数调用的参数个数不变），带括号路径的占位由「整�? 0」改�? `Missing`；cgen �? COM 晚绑定路径改为按形参位置展开参数表，省略位置生成 `vb6_ComPackMissing()`；RTL 新增 `vb6_ComPackMissing()`（VT_ERROR + DISP_E_PARAMNOTFOUND），并在用户侧头 `vb6rtl_class_com.h` 声明 —�? 缺该声明会被 C 编译器隐式按 int 返回处理，x64 下指针截断成 32 位立即访问违例，首轮实测即崩在此�?**验证**：`c.Add "x", , , 1` 生成 4 个实�? `{x, Missing, Missing, 1}`；探�? `afterInsert=axbc`、`Before=1 �? ya`、`Key+After` 三例全部正确�? Err=0；全量回�? PASS=82 / FAIL=0 / SKIP=1 / TOTAL=83 零回�?
- 2026-09-18（新�? `src/rtl/core/vb6com/vb6com_collection.c` + `vb6com_collection_enum.c`；改 `vb6com.c`、`src/rtl/core/vb6rtl/vb6rtl_conv.c`、`src/driver/{c3rtl.rc,rtl_embedded.hpp,rtl_embedded.cpp,driver_link.cpp}`、`CMakeLists.txt`）：**新增 VB6 内建对象 `Collection` 的运行时实现** —�? `New Collection` / `Dim c As New Collection` 生成 `vb6_NewObject(L"Collection")`，�? `Collection` �? `VBA.Collection` 都不注册 ProgID（实�? `CLSIDFromProgID` 两者均 `0x800401F3`），RTL 也没有内建实�? �? 只要用到集合就必�? 429。改为在 RTL 侧手�? IDispatch 对象：实�? `Add`（Item/[Key]/[Before]/[After] 四个形参，含按键与按位置插入）、`Item`（默认成�? `DISPID_VALUE`，支�? `c(1)` / `c!key`）、`Count`、`Remove`、`_NewEnum`（IEnumVARIANT，接既有 `vb6com_foreach.c` �? For Each 通路），键重复抛 457、键不存在抛 5、序号越界抛 9；错误描述复�? `vb6_StdErrorDesc`（该函数�? `static` 提升为外部链接）。`vb6_NewObject` 改为**先探内建（`vb6_NewBuiltinObject`）、未命中再回退 COM 注册�?**，不影响其它类名的既有行为。RTL 5 处管线同步（rc 75 �? **77 �?** = 23 .h + 48 .c + 6 .inc；`RtlResourceID` 枚举、`files[]`、`driver_link.cpp` �? `sourceFiles`、CMake `C3RTL_EMBEDDED_FILES`）�?**新文件按台账「单文件 �?500 行」口径拆成两个编译单�?**（容�? + IDispatch 411 �? / 枚举�? 105 行），跨文件共享的结构体与枚举器 vtable 放进既有�? `vb6com_internal.h`（未新增头文件）�?**验证**：探�? 14 项（创建 / Add / Count / Item 序号 / Item �? / 默认成员 / For Each / Remove 序号 / Remove �? / After 插入 / Before 插入 / 重复键→457 / 键不存在�?5 / 释放）全部正确且退出码 0；两个新编译单元 cl `/W3` 零警告；解包目录出现两个新文件并各自产出 .obj；全量回归零回归
- 2026-09-18（src/backend/expr/cgen_expr.cpp）：**修早绑定 `New` 查表用裸模块名导�? ProgID 丢类型库前缀** —�? `visit(NewExpr)` 原用 `symTab_.lookupModule(node.className)` �?**全名**查符号，而类型库解析�? coclass �?**裸名**登记（`WinHttpRequest` / `Dictionary`），源码里写的是限定名（`WinHttp.WinHttpRequest`）→ 查表必然落空 �? 落到 `else` 分支把源码限定名�? ProgID 交给 `vb6_NewObject`；�? `WinHttp` 只注册了版本化名 `WinHttp.WinHttpRequest.5.1`，裸名无注册 �? `CLSIDFromProgID` 返回 `0x800401F3` �? 运行�? 429（宿主页面报 `WinHttp.WinHttpRequest` 错误）。改�? `lookupModuleDotted`（与 `cgen_with.cpp` �? `NewExpr` 处理、`cgen_decl_var.cpp` 的类型化 COM 登记一致），命�? `SymbolKind::Class` 后改用类型库 `ProgIDFromCLSID` 反查到的真实 ProgID�?**验证**：`--emit-c` �? `vb6_NewObject(L"WinHttp.WinHttpRequest")` 变为 `L"WinHttp.WinHttpRequest.5.1"`；修复后 x64 �? x86 产物二进制扫描裸 ProgID 残留均为 0；`cHttpServer` / `cHttpClient` / `cRedisClient` �? `CreateInstance` + `Release` 不再�? 429；全量回归零回归
- 2026-09-18（src/semantics/symbol_table.hpp、semantic_analyzer.cpp、src/driver/driver_crossmod.cpp、src/backend/module/cgen_com.cpp、src/backend/detail/util/cgen_util_dllentry_{prelude,collect,tables}.inc、src/driver/detail/driver_codegen_dll_typelib.inc、src/backend/detail/base/cgen_base_generate_{c_open,body_pass}.inc、src/backend/detail/util/cgen_helpers.inc、src/rtl/core/vb6comserver/vb6comserver{,_obj}.{h,c}）：**�? ActiveX DLL �? Public 数据字段不进 IDispatch �?** —�? 生成�? `g_vb6_disp_<cls>Methods[]` 只收 Public 方法�? Property，`Public Router As cHttpServerRouter` 这类数据字段没有表项（`L"Router"` 出现 0 次），宿�? `GetIDsOfNames("Router")` 返回 `DISP_E_UNKNOWNNAME 0x80020006` �? `vb6_ComGetProp` �? NULL �? 调用方无空值检查即段错误。改为：语义阶段只收�?**显式 `Public` 关键�?**的数据字段（本编译器�? `Dim` 解析�? Public，而真 VB6 �? `Dim` 等价 Private，故不能按访问级判定）→ TypeLib 阶段按字段类型分�? DISPID 并回�? �? 后端每个字段生成 Get(invkind 2)/Let(invkind 4) 两条共享同一 DISPID 的表�? + 取�?/赋值桥接函数；RTL 新增 `vb6_ComObject_FromInstance`/`vb6_FindCoClassDesc`/`vb6_ComObject_GetInstance`，供工程内类字段�? VT_DISPATCH 回传�?**验证**：vbman�?129 模块 / 104 coclass）全量编译出 x64 DLL；`DllGetClassObject`+`CreateInstance` 后对 cHttpServer �? `Router/SSE/Database/Statistics/RouteBefore/RouteAfter/LastError/MaxConnections/ScriptEngine/CrossDomain` 逐个 `GetIDsOfNames` �? **10/10 成功**（修复前 0/10），DISPID 为真实值（Router=176、SSE=174…），防御段 9000+ 残留 0，`Invoke(Router, PROPERTYGET)` 返回 `vt=9`(VT_DISPATCH) 且指针非空；全量回归 PASS=82 / FAIL=0 / SKIP=1 / TOTAL=83 与基线一�?
- 2026-09-18（src/backend/expr/cgen_expr.cpp）：**�? `Not <比较表达�?>` 恒为�?** —�? `Not` 一律按位取�? `~(int32_t)(x)`，但比较运算生成的是 C �? 0/1（不�? VB6 布尔 -1/0），于是 `~1 = -2`、`~0 = -1` 两者都非零 �? `Not (1=1)`、`Not x Is Nothing`、`Not TypeOf �? Is …` 全部为真。改为：操作数是比较（`Eq/Neq/Lt/Gt/Le/Ge/Like/Is`）、`TypeOf` 或嵌�? `Not` 时改用逻辑取反 `(((int32_t)(x) != 0) ? 0 : -1)`，数值操作数仍走位取反�?**验证**：隔离用�? `If Not o Is Nothing`（o=Nothing）与 `If Not (1=1)` 由「错误进分支」变为「正确跳过」，`Not (1=2)`、`o Is Nothing` 保持正确；全量回�? PASS=82 / FAIL=0 / SKIP=1 / TOTAL=83 零回归�?**顺带解开一�?**：vbman `cHttpServer.StopMe` �? `If Not m_oServer Is Nothing Then m_oServer.CloseSck` 原本恒进分支、对未赋值的 `m_oServer` �? `CloseSck(NULL)` �? 释放实例�? 0xC0000005（栈：`ComObj_Release` �? `vb6_cls_cHttpServer_Destroy` �? `Class_Terminate` �? `StopMe` �? `vb6_cTlsReMaster_CloseSck` �? `vb6_cWinsock_prop_get_State`），修后该处不再触发
- 2026-09-18（已�? Fix 101，最小改动）�?**内部工程类字段的 `Set X = Nothing` 走了 COM 释放路径** —�? `src/backend/detail/stmt/cgen_setlet_set_prop.inc` 对「项目类实例」本应生�? `target = NULL;`（注释已写明�? `vb6_ReleaseObject` 释放会对�? C 结构体走 vtable Release �? 0xC0000005），但判定条件是 `knownClassVars_.count(targetLower)`，�? `knownClassVars_` �?**裸变量名**登记（`sse`），类模块内的目标表达式却是 `me-><字段>`（`me->sse`）→ 永不命中 �? 落到 `vb6_ReleaseObject` �? 把纯 C 结构体当 `IUnknown*` 解引用首成员 `__comObj`（恒 NULL）→ 崩�?**实测**：x64 DLL `CreateInstance(cHttpServer)` �? `Release` 必崩（`Class_Terminate` 第一�? `vb6_ReleaseObject(&me->SSE)`），`cRedisClient` 同样；`cCsv/cDelay/cTimer/cCollection`（Class_Initialize 不创建内部类字段）正常释放�?**对照排除回归**：未�? Fix 099 �? x86 旧构建「纯 CreateInstance + Release、完全不�? Invoke」同样崩，故�? Fix 099/Fix 100 无关�?**修法**（`src/backend/detail/stmt/cgen_setlet_set_prop.inc`，最小改动）：判定条件由 `knownClassVars_.count(targetLower)` 扩为「命中裸变量�? **�?** `inferClassTypeOfExpr(*node.target)` 推得出项目类」，补上�?**表达式类型推�?**识别工程类实例这一路；`inferClassTypeOfExpr` 递归覆盖 `MeExpr` / `me->字段` / With 成员 / 方法返回类，且对 `classTypedFieldMap_` 中标�? `COM:` 的字段与 `SymbolKind::ComClass` 一律返回空�? —�? 类型�? COM 仍走 `vb6_ComReleaseTyped`、其余仍�? `vb6_ReleaseObject`，不误伤；函数调用形态（`isCallTarget`）显式排除（其求值非左值，生成 `<call> = NULL` �? C2106/C2440）�?**验证**：vbman x64 DLL `CreateInstance(cHttpServer)` / `CreateInstance(cRedisClient)` �? `Release` 不再崩（8 个工程类字段改为 `= NULL`），同工程内 2 �? COM `Dictionary` 字段仍走 `vb6_ReleaseObject` 未被误改；全量回归零回归
- 2026-09-18（src/com/typelib_parser_parse.cpp、src/com/typelib_parser.hpp）：**修早绑定对象 ProgID 丢类型库前缀** —�? `parseCoClass` �? `ProgIDFromCLSID` 反查失败时回退�?**裸类�?**，运行期 `CLSIDFromProgID` 必然查不到（组件未注册、或 32 位组�? + 64 位宿主进程时反查必失败，属常态路径）�? VB6 错误 429 / `0x800401F3`。改为回退�? `<TypeLib库名>.<coclass�?>`（VB6 约定；实测三例全部符合：`VBMAN.dll`→`VBMANLIB`、`stdole2.tlb`→`stdole`、`msado28.tlb`→`ADODB`）；库名�? `parseTypeLib` 存入新增私有成员 `currentProjectName_`�?**验证**：vbman-demo �? Helloworld 生成代码�? `vb6_NewObject(L"cHttpServer")` 变为 `vb6_NewObject(L"VBMANLIB.cHttpServer")`、`vb6_CreateObject(L"sGlobal")` 变为 `L"VBMANLIB.sGlobal"`，裸 ProgID 残留 0
- 2026-09-18（src/backend/detail/util/cgen_util_dllentry_exports.inc）：**�? DllRegisterServer 首个 coclass 失败即整轮中�?** —�? �? `if (FAILED(hr)) return hr;` 会让其后所�? coclass 全部跳过、且 `vb6_RegisterTypeLib` **永不执行**（TypeLib 完全缺失）；改为累计失败数、循环跑完、TypeLib 无条件注册，返回值仍如实反映失败（非提权调用时实�? `E_FAIL 0x80004005`，旧代码在第 1 个类就退出）�?**验证**：提权调�? C3 �? `VBMAN.dll` �? `DllRegisterServer` �? `HRESULT 0x00000000`�?32 位视�? `Wow6432Node\CLSID` �? **104/104** �? CLSID 指向�? DLL、`VBMANLIB.*` ProgID 全部就位。附更正：此前记录的「只注册 13/104」是**测量假象**（非提权运行 + 注册表视图读错），逐类 CLSID 实测格式合法且唯一，无失败�?
- 2026-09-18（Fix 094 零回归复验，tests/regress_all.ps1 全量对照）：Fix 094 改的�? P24-04 �? COM 符号注册路径（影响面覆盖所有类型库），故改完重跑全�? —�? 117 用例与基�? `report-20260917-235836.csv` 逐条比对**变化总数 0**，判定分布完全一致（BOTH-FAIL 10 / C3-EXT 29 / COMPILE-PASS 12 / PASS 47 / RUN-TIMEOUT 3 / SKIP-AUX 16），报告 `tests/_vb6ref/report-20260918-133707.csv`。附：Fix 094 前那轮（`report-20260918-130528.csv`）有 2 项变化，均为模态框是否被人点掉所致（`test_onerror.bas` RUN-TIMEOUT→PASS、`P6.12Client` RUN-TIMEOUT→RUN-DIFF），非代码回�?
- 2026-09-18（零回归验证，tests/regress_all.ps1 全量对照）：本轮两处修复后跑 117 用例，与改动前基�? `report-20260917-235836.csv` 逐条比对�?**�? 2 个用�? Verdict 变化且均非代码回�?** —�? `test_onerror.bas` RUN-TIMEOUT→PASS（真 VB6 侧弹「Run-time error '53'」模态框，人工点掉后不再超时；C3 侧始�? exit=0、输�? `PASS1/PASS2/PASS3a/PASS3b/Done`）、`test_com_client__P6.12Client.bas` RUN-TIMEOUT→RUN-DIFF（`TestAXDLL.Calc` 未注册，C3 �? CreateObject 失败返回 429、VB6 侧点掉弹窗后 exit=0）；其余 115 �? Verdict 与基�?**逐条一�?**。附：C3 产出�? `test_activex_dll.dll`（x64）经 HKCU\Software\Classes 注册后，C3 编的 `P6.12Client.exe` 跑出 `6/6 PASSED`（exit=0），注销后残�? 0
- 2026-09-18（已修，运行期冒烟实测揪出）�?**C3 产物 COM 晚绑定方法调用崩 0xC0000005** —�? 晚绑定宿�? `tests/vbman_host`（纯 `CreateObject("VBMANLIB.cVBMAN")` + `IDispatch`，不声明任何类型库引用）�? `tests/run_vbman_test.ps1` 下：`CreateObject` 成功（输�? `create=OK`），随即 `vm.Version()` 崩，**x86 �? x64 均复�?**（`0xC0000005 ACCESS_VIOLATION`）；**对照基线**用真 VB6 产出�? `tests/vbman/dist/DLL/VBMAN.dll`�?2,306,048 字节）跑同一宿主 �? `PASS`、退出码 `0x00000000`、完整输�? `begin/create=OK/version=OK/create2=OK/ok=3/end`�?**证明是本机环境无关的 C3 缺陷**（原因在 `IDispatch::Invoke` �? DISPPARAMS 构�? / `Optional HostApp As Object` 缺省值填充）�?**已修**（`src/backend/detail/util/cgen_util_dllentry_collect.inc`，Fix 093，两层）：① 桥接函数每个参数�? `(argc > i)` 守卫 —�? 零参调用�? `args == NULL`，原代码无条件解引用 `((VARIANT*)args[0])->lVal` 必崩；`_has_<name>` 标志由恒 `1` 改为按实际实参个数。② `Vb6Type::Object` �? ByRef 分支�? `&lVal`（`LONG*`�?4 字节）改�? `(void*)&pdispVal`（指针字段的真实地址），ByVal 分支�? `lVal` 改为 `pdispVal` —�? x86 �? 4 字节�? `void*` 同宽侥幸可用，x64 下被�? `void**` 解引用会�? 8 字节越过该字段、读到栈垃圾�? `IDispatch*`（这正是「x86 修完 PASS、x64 仍崩」的原因）�?**验证**：`tests/run_vbman_test.ps1` 实测 x86 �? x64 **双双 PASS**（退出码 `0x00000000`，完整输�? `begin/create=OK/version=OK/create2=OK/ok=3/end`），注销后注册残�? 0
- 2026-09-18（已修）�?**GlobalNameSpace 早绑定生成未定义符号** —�? `tests/test_vbman/Module1.bas` �? `VBMAN.Version()`（`sGlobal.cls` �? `Attribute VB_GlobalNameSpace = True`）被 C3 生成�? `vb6_BSTR_Assign(&v, vb6_VBMAN_Version());`，�? `_vb6_VBMAN_Version` 无定�? �? `error LNK2019: 无法解析的外部符号`�?**原因**：类型库加载失败时全程静默降�? —�? `src/com/typelib_parser.cpp` �? `loadByClsid` 两处失败分支静默 `return nullptr`，`src/driver/driver_compile.cpp` �? `Reference=` 路径不存在时静默丢弃，P24-04 的符号注册循环被跳过，`VBMAN.Version` 落入 M22「模块名.成员」分支（`cgen_expr_member_m22_module.inc` �? 103 行）无条件拼 `vb6_<限定�?>_<成员>`�?**已修**（诊断补齐，最小改动，不动降级行为以免误伤合法的「模块名.方法」用法）：三处静默丢弃点各补一�? `warning VB4001`，把排查方向直接指向「类型库未注�? / 引用路径无效」�?**验证**：编�? `tests/test_vbman/test_vbman.vbp` 实测输出 `(1,1): warning VB4001: TypeLib reference path not found: C:\VBMANLIB\_bin\DLL\VBMAN.dll` �? `(1,1): warning VB4001: TypeLib not registered for CLSID/LibID: {A638B373-E3B1-4281-8BD3-957EAD54B31C}`。注：该工程 `Reference=` 指向不存在的 `C:\VBMANLIB\_bin\DLL\VBMAN.dll`（旧路径）属工程配置；类型库可用�? P24-04 正常�?**Fix 094（真正的原因，`src/driver/driver_semantics.cpp`�?**：P24-04 提升 GlobalNameSpace 方法时用 `size_t standardMethods = isDispatch ? 7 : 3; for (mi = standardMethods; mi < members.size(); mi++)` —�? 假设「接�? members 的前 N 个是 IUnknown/IDispatch 标准方法」。该假设只对**�? VB6 生成�? TypeLib** 成立�?**C3 自产�? TypeLib 接口 members 只含业务方法**（实�? `sGlobal._sGlobal` members=1 只有 `VBMAN`）→ `mi=7 < 1` 循环不执�? �? 一个方法都提升不了 �? `VBMAN.Version()` 退化成 `vb6_VBMAN_Version()` �? LNK2019（上面的诊断只覆盖「路径不存在 / GUID 未注册」，覆盖不到这条 —�? 实测 Reference 指向**存在�?** C3 自产 VBMAN.dll 时同�? LNK2019）。改�?**按名过滤**标准方法（`QueryInterface`/`AddRef`/`Release`/`GetTypeInfoCount`/`GetTypeInfo`/`GetIDsOfNames`/`Invoke`），�? VB6 �? C3 自产两种 TypeLib 都能正确提升�?**验证**：`tmp/gns_probe/test_gns.vbp`（Reference 指向 C3 自产 VBMAN.dll）由 `vb6_VBMAN_Version()` 变为 `vb6_ComCallBSTR(vb6_ComCallObject(vb6_CreateObject(L"sGlobal"), L"VBMAN", NULL, 0), L"Version", NULL, 0)`，真实编译产�? `test_gns.exe`�?177,152 字节，LNK2019 消失�?
- 2026-09-18（tests/run_vbman_test.ps1、tests/vbman_host）：**新增 vbman 运行期冒烟测�?**（与 `regress_vbman.ps1` 的编译期对照是两个维度）—�? 流程「C3 现编 VBMAN.dll（x86/x64 分目录）�? 按架构选对�? `regsvr32`（x64 �? `System32`�?32 位进程下�? `Sysnative`；x86 �? `SysWOW64`）注�? �? 改写宿主 vbp 类型库引�? �? 编宿�? �? 跑宿�? �? 断言 stdout �? **finally 必注销并复核残�?**」；判定矩阵 `PASS / BUILD-FAIL / ARCH-DIFF / REG-FAIL / HANG / CRASH / NO-OUTPUT / ASSERT-FAIL / UNREG-FAIL / SCRIPT-ERROR`；带 PE machine 守卫（`0x014C`/`0x8664`，拦 C3 静默降级）；`-LibDll` 可跳过编译直接测现成 DLL（跑对照基线）；零硬编码，路径走 `C3_EXE`/`C3_VBMAN_RUNROOT`/`C3_VBMAN_LIB_VBP`/`C3_VBMAN_HOST_VBP`/`C3_VCVARSALL` 或参�?
  - 自提权实现注记：本机实测**提权子进程跑完不退�?**（`Start-Process -Verb RunAs -Wait` 永久卡住），故父进程改认 sentinel 文件 `elevated-<token>.done.txt`（`Exit-With` �? `Stop-Transcript` 后写），拿到�? `Stop-Process` 主动收掉；token 用时间戳保证文件名唯一�?**彻底不依赖任何删除动�?**（沙�? safe-delete 守卫会静默拦 `Remove-Item`，曾导致读到陈旧 sentinel、第二轮秒退�?
  - 实测结论：`ParseErrors=0`、提权链路稳定（2m33s�?2m44s）、真 VB6 产物 `PASS`、C3 �? x86/x64 �? `CRASH 0xC0000005`�?**两轮跑完系统注册残留均为 0**
- 2026-09-18（src/driver/driver_semantics.cpp）：�? COM get/put 同名覆盖缺陷 —�? 类型库里 VB6 公开字段与读写属性是 `get_X`+`put_X` 一对同 memid 同名 FUNCDESC，�? `comMethods` 按小写名索引，后写入�? put/putref 会覆�? getter 的返回类型（退化成 Empty）与 `isPropertyGet`，使 `ctx.Request.QueryString(...)` 被判成标量取值再�? C 成员访问�?4 �? C2224）；改为 setter 不覆盖已�? getter（读取只�? getter，写入走名字化晚绑定不查此表）。修�? `tests/vbman-demo/demos/Helloworld` �? x86 �? x64 均编译通过（此前只有真 VB6 能编），全量回归 82/0/1/83 与基线一�?
- 2026-09-18（tests/vbman-demo）：新增 vbman-demo 子模块（`git@gitcode.com:woeoio/vbman-demo.git`，master�?377 文件 / 56MB，`demos/` �? 20+ �? .vbp 示例工程）；`regress_all.ps1` 的排除模�? `.*vbman.*` 按子串匹配，会自动跳过它，无需改脚�?
- 2026-09-18（src/backend/msvc_driver.*、src/driver/driver_link.cpp）：C3 �? shell-out 全部改走 CREATE_NO_WINDOW 隐藏窗口（不再弹 cmd 窗口）；rc.exe 两处调用补回外层引号，否�? cmd 剥引号破�? rc 路径、TypeLib/版本资源静默丢失（实�? DLL �? 141KB�?
- 2026-09-18（tests/regress_vbman.ps1）：新增 vbman 专项对照回归脚本（regress_all 的补集，只测 `src\VBMAN.vbp`；OleDll 用导出表比对替代运行比对；VB6 侧在副本里编译，不再弄脏子模块）
- 2026-09-17（版本起手）：分�? `ferock/0.10.6` �? `ferock/0.10.5`（`3817c67`）起；VERSION �? ` 0.10.6`（唯一权威源，行首空格 + CRLF 保持�?
- 2026-09-17 修复 3 项既有缺陷（0.10.5 对照回归 `tests/regress_all.ps1` 实测揪出，全部与源码拆分无关）：
  1. **vb6_CurDir 0 参调�? C2198**（test_sysfunc）：`cgen_expr_ident_builtin.inc` �? `zeroArgBuiltinFuncs` 路径�? `curdir` 直接�? `vb6_CurDir()`，�? RTL 签名�? `vb6_CurDir(BSTR drive)`——ident 路径不经�? call 路径�? P14.2.2 fixup（`Dir` 之所以正常是因其不在该集合）。修复：ident 路径特判 `curdir` �? `vb6_CurDir(NULL)`（RTL �? else 分支本就健壮处理 NULL=当前驱动器）；同集合其余函数（now/date/time/command/timer 等）签名逐一核对均无此问�?
  2. **vb6_vsink_http_create C2197**（test_com_events_winhttp）：`cgen_com_events.cpp` `emitComVtableSinkDecls` 生成声明 `void* vb6_vsink_<var>_create(void)`，而定义（`emitComVtableSinks`）与调用�? `void* handler` 带参——声明与定义不一致。修复：声明改为 `(void* handler)`
  3. **标准模块�? Print LNK2019**（test_concat_leak/stress、test_p613_typelib）：C3 扩展语义接受�? GUI 工程�? `Print`（生�? `vb6_Form_Print(NULL, ...)`），�? `driver_link.cpp` 仅在 `isGui||isDll` 时链 vb6forms 系列 �? LNK2019。修复：forms 源清单抽�? `addFormsSources()`，非 GUI/DLL 时扫描生成的 .c 源码、命�? `vb6_Form_` 前缀即补链（�? VB6 拒绝该语法，判定仍属 C3-EXT，但 C3 自己生成的代码必须能链接�?
- 2026-09-17 验证：构�? 0 错误�?5 个目标用例全过（test_sysfunc �?+�? rc=0/0 输出正常、concat_leak/stress、p613_typelib �?+�? rc=0/0、winhttp 编译 rc=0）；全量回归 **82/0/1/83** 与基线一致（修复零回归）

- 2026-09-17（版本起手）：分�? `ferock/0.10.5` �? main（`66b2791`）起；按 README「版本号唯一权威源」只改根目录 `VERSION`（保持行首空�? + CRLF）→ ` 0.10.5`，其余使用方（CMake `file(READ)` / 编译期宏 / `release.bat`）自动跟随；重建�? `C3 --version` = 0.10.5�?6 项静态自检全过
- 2026-09-17 源码拆分（第 3 个「纯搬移 + 函数体片段」两步法）：driver.cpp 2494→主 121 —�? �? 纯搬�? 9 个函数共 1713 行到 6 个新编译单元（均已登�? CMakeLists）：driver_args 232（parseArgs / compile(int,char**) / writeErrorLog / printHelp / VB6C3_VERSION_STRING 兜底�? + printVersion）、driver_compile 384（compile(int,char**) + compile(CompileOptions)）、driver_frontend 340（runLexer / runPreprocess / runParser / runTypeLibImport）、driver_semantics 339（runSemanticAnalysis）、driver_crossmod 169（runCrossModuleResolution）、driver_link 358（runLinker）；`class ExternalRefCollector` **留原文件**（被 runCodeGeneration 内使用，须同编译单元）。② runCodeGeneration 函数�? 678 行按既有 Fix/P6.x/M29 分节注释�? 9 片段�? `src/driver/detail/`（prelude 5 / voidfield_scan 88 / typedfield_scan 84 / variant_funcs 70 / dup_module_vars 29 / module_loop 92 / dll_typelib 236 / dll_sync 55 / dll_entry 19），其中 dll_* 三段位于 `if (options.isDll �?)` 块内（相对花括号深度 1，片段自身不闭合但拼接后逐行一致）�?9 �? .inc 不登�? CMakeLists。验证：�? 搬移三重断言（非空行全覆盖不重叠 / 回读比对 / �? driver.cpp 非空行序列一致，仅空行分隔归一）；�? 片段**独立复算**（用拆分前备份，不依赖拆分脚本自身断言）：9 片段去头后与原文区间逐行一�? + 伞文件结构（16 行说�? + 原文�? 94 �? + 9 include + 原文�? 3 行）；③ 增量构建零错误（首轮 3 个新单元�? `C2027 使用了未定义类型 Module / SemanticAnalyzer` �? �? `ast/ast.hpp`、`semantics/semantic_analyzer.hpp`；第 2 步只重编 driver.cpp + 链接，无新增编译单元）；�? **生成代码对照：拆分前后各�? 100 个样例（92 .bas + 8 .frm）跑 --emit-c�?12635 行逐字节一致（100/100，diff -rq 无输出、退出码序列一致、stderr 全空�?**；⑤ 回归 82/0/1/83。src �? �?500 行文�? 2 �? 1（仅�? vb6rtl.c 5327�?
- 2026-09-17（环境注记，新）：`emit_c_snapshot.sh` 开头的 `rm -rf "$OUT"` 在本沙箱被安全层截成 trash �?**失败**（`SAFE_DELETE_FAIL_CLOSED`）→ 输出目录未清空，二次采集�?**追加**到旧 `_rc.txt`，表现为 `wc -l` = 200 而实际样�? 100。判据改�? `awk 'NF' _rc.txt | sort -u | wc -l == 100`；比对仍�? `diff -rq` 与逐文�? `cmp` 为准
- 2026-09-17 源码拆分（最后一�? �?500 文件，RTL）：vb6rtl.c 5327→主 418 + **13 个族编译单元**（string 489 / format 46 / conv 336 / misc 400 / system 477 / compat 413 / date 468 / array 461 / file 467 / paramarray 289 / financial 189 / com 494 / registry 186，最�? 494；族区间映射 13 条写在主文件头注释），主文件保留初始�?/退�? + 内存/Variant + 错误处理；`vb6_Format`�?608）所�? format 族再按函数体片段�? 6 �? .inc 进内�? `detail/`（extract 59 / parse 120 / numeric_pre 109 / numeric_body 138 / numeric_tail 104 / string 100，include 一�? basename——解包后平铺）�?**台账原预判�?32 �? static 须先�? internal.h」实测过�?**：static 随唯一使用者族搬走保持 static（g_formList→system、错误栈留主），未�? internal.h；中�? #define 随使用者进族（VB6_MAX_FORMS→system、VB6_MAX_FILES→file）、setjmp.h 随错误处理留主文件。RTL 5 处管线同步：rc 56�?**75 �?**�?23 .h + 46 .c + 6 .inc）、枚举、files[]、driver_link.cpp �? sourceFiles�?14 �? .c 逐个列举）、CMake 75 项—�?**RTL �? .inc 片段必须进嵌入管线（�? C++ �? .inc 不登�? CMake 相反�?**。验证：独立复算 14/14 + 构建 0 错误 + e2e（hello.exe / test_activex_dll --dll；RTL 批次�? --emit-c 对照可用�?+ 回归 82/0/1/83；拆分前�? exe �? ~65% 字节�? TU 布局变化（同版本两次构建仅差 2 字节 PE 时间戳）。src �? �?500 行文�? 1 �? 0
- 2026-09-17 目录整理（用户要求「别都平铺」）+ 口径修正：backend/detail 55 个平�? .inc 按前缀收进 5 子目录（base 11 / expr 23 / module 8 / stmt 6 / util 7），167 �? include 改写；RTL format 6 片段收进 core/vb6rtl/detail/�?**`.inc` 计入 �?500 行口�?**（此前漏算，发现 cgen_form_create_controls.inc 503 行）�? 再切一刀�?207（内�?=cgen_form.cpp 1272~1475�?+ 新建 cgen_form_ctrl_style_apply.inc 300（内�?=1476~1771）。验证：emit-c 快照 100/100 逐字节一致（12635 行）�?167 �? include 全可解析（build 三连 0 错误）、再切独立复算通过�?**最终实测：src �? �?500 行文件（�? .inc�?= 0 个，最�? 494 �?**

### 0.10.4

- 2026-09-17 源码拆分（第 2 个「纯搬移 + 函数体片段」两步法）：cgen_base 1753→主 124 —�? �? 纯搬�? 16 个函数到 2 个新编译单元（均已登�? CMakeLists）：backend/cgen_base_type.cpp 398 行（mapType / mapComType / emitGuidInitializer / mapTypeRef / mapDeclareType / tryEvalConstInt）、backend/cgen_base_naming.cpp 225 行（defaultValue / cIdent / resolveArrayTargetIdent / lookupConstSym / isStringConstIdent / isConstIdent / constIdentType / wrapConstArgForByRef / cProcName�?+ 自由函数 controlTypeFromName；主文件 1753 �? 1160。② generate（原 1067 行）函数体按既有 `// ---- X` 分节注释�? 11 个片段到 backend/detail/（prologue 45 / state_scan 128 / header_open 63 / crossmod 74 / c_open 117 / decl_pass 103 / evt_decl 80 / body_pass 127 / evt_impl 163 / entry 151 / epilogue 55，含头注释），主文件 1160 �? 124�?11 �? .inc 不登�? CMakeLists。controlTypeFromName 的两处局部前瞻声明（cgen_decl.cpp / cgen_decl_var.cpp �? 12 行）无需改动。验证：�? 两步各断言「拼回原文件逐行一致」（�? 2 �? 13 项校验全过，�? git HEAD 原文独立复算）；�? 增量构建零错误；�? **生成代码对照：拆分前�? `git archive HEAD` 导出源码树单独构建的 C3.exe�?0.10.4）与拆分后各�? 100 个样例（92 �? .bas + 8 �? .frm）跑 --emit-c�?12635 行逐字节一致（100/100，stderr 全空、退出码序列一致）**；④ 回归 82/0/1/83。src �? �?500 行文�? 3 �? 2（仅�? driver.cpp 2494、vb6rtl.c 5327�?
- 2026-09-17（工具修复）：skill c3_giant_func_split �? split_func_body.py 在「片段区间末行恰为空行」时落盘会丢那一行（拼回断言用内存数据，查不出来）—�? 已改为写入时统一�? `\r\n` 收尾；并把该现象与「函数签名跨行时 --depth 0 扫不出候选点」写�? SKILL.md 已知�?
- 2026-09-17 源码拆分（「伞头拆分」，�? 4 个头文件）：vb6forms.h 582 �? 伞头 23 �? + 同目�? 9 个子头（vb6forms_window.h 142 / _prop.h 77 / _prop_ctrl.h 140 / _prop_pic.h 65 / _prop_form.h 86 / _ctrlarr.h 54 / _mdi.h 48 / _webview.h 61 / _controls.h 27，均含各自头尾）。切点取文件内既�? banner（窗体框架·控件创建·Timer·消息循环·事件桥接·工具函数·Unload / 控件属性读�? / 控件数组 / MDI / WebView2 / 动态加载控件）�? 204~449 那段�? banner 长块里的 `// P13.x / P17.2 / P18-F / P20-xx` 注释边界，覆盖原�? 15~579 行；伞头�? BOM、子头无 BOM（与 vb6rtl_*.h 口径一致）。RTL 管线同步 4 处（.h 不进 driver.cpp �? sourceFiles）：c3rtl.rc 新增 147~155 且头注释 47 �? 56 项、RtlResourceID 枚举、files[]、CMakeLists �? C3RTL_EMBEDDED_FILES。验证：�? 9 个子头去头后�? git HEAD 版原文对应区间逐行一致（�? HEAD 原文独立复算）、拼�? == 原文件第 15~579 行；�? 全量重建成功（重�? rc + rtl_embedded.cpp + driver.cpp）；�? 端到�? hello.bas(→hello.exe，运行输�? Count: 1..8、退出码 0) + test_activex_dll.vbp(--dll) 通过（回归不覆盖该路径）；④ 四处清单计数一�? 56/56/56/56，解�? session 目录 56 个文件与源码树逐字节一致；�? 回归 82/0/1/83。src �? �?500 行文�? 4 �? 3
- 2026-09-17 源码拆分（「函数体片段」，本工程单文件最大）：cgen_expr_call 3627→主 46 + backend/detail/ 12 片段（prelude 287 / builtin_pre 321 / callee_ident 185 / callee_member 204 / callee_withm 158 / com_bind 386 / callee_params 217 / arg_emit 366 / arg_variant 317 / named_args 407 / builtin_fixup 390 / pad_conv 373，正文行数，�? +4 行头注释故最�? 411）。该文件唯一顶层函数 visit(IndexOrCallExpr&) 占函数体 3611 行；切点全取既有�? `// ---- Fix/Pxxxx` 分节注释与顶层语句边界（台账原估 7~8 段，实测按语义家族切 12 段）�?**arg_emit / arg_variant 两段位于 1771 行那个遍历位置实参的 for 循环内部（相对花括号深度 1�?**，首次把巨型 for 循环体也切片；片段自身不闭合但拼回逐行一致�?12 �? .inc 不登�? CMakeLists（非编译单元）。验证：�? 12 段去头后与原文区间逐行一致、拼�? == 原文件（3627 行，独立复算）；�? 增量构建只重�? 1 个目标、无新增编译单元；③ **生成代码对照：拆分前后各�? 100 个样例（92 �? .bas + 8 �? .frm）跑 --emit-c�?12635 行逐字节一致（100/100，stderr 全空、退出码序列一致）**；④ 回归 82/0/1/83。src �? �?500 行文�? 5 �? 4
- 2026-09-17 源码拆分（首个「纯搬移 + 函数体片段」两步法）：cgen_form 2222→主 86 —�? �? 纯搬�? 4 �? CCodeGen 成员函数到新编译单元 backend/module/cgen_form_menu.cpp�?228 行，已登�? CMakeLists）：emitMenuItem 68 / emitMenuClickDispatch 81 / escapeWideCString 46 / CCodeGen::escapeCString 15；② 余下 emitFormFramework（单函数 1945）的函数体按既有分节注释�? 8 个片段到 backend/detail/（prelude 91 / ctrl_registry 117 / wndproc_subclass 336 / wndproc_create 324 / wndproc_dispatch 351 / create_controls 500 / frame_menu 165 / show 59），切点全在「相对花括号深度 0」。两个文件级 static（bytesToHexArray / �? escapeCString）留在原位（内部链接、调用点全在本函数内）。验证：搬移与切片各断言「拼回原文件逐行一致」；增量构建零错误；回归 82/0/1/83�?8 �? .frm 编译用例�? PASS）；--dump-ast 18 样例逐字节一�? 18/18；另做生成代码对�? —�? 拆分前（HEAD 版）C3 与拆分后 C3 �? 8 �? .frm �? --emit-c�?1769 行逐字节一�? 8/8。src �? �?500 行文�? 6 �? 5
- 2026-09-17（验证手法，新）：`--emit-c` 只把生成�? C 代码打到 stdout、不�? cl.exe，可用于「拆分前后生成代码对照」，且不受本�? reg.exe 黑名单影响；注意 `git show HEAD:<file>` 输出�? LF，写回工作区前要 `sed 's/$/\r/'` �? CRLF
- 2026-09-17（版本号根治）：版本号收敛到 `VERSION` 唯一权威源，消除 2 处硬编码副本 —�? `CMakeLists.txt` 改为 `file(READ "${CMAKE_CURRENT_LIST_DIR}/VERSION")` + `string(STRIP)` 喂给 `project()`，并�? `set_property(DIRECTORY APPEND PROPERTY CMAKE_CONFIGURE_DEPENDS .../VERSION)` 保证�? VERSION 会自动重配置�?**缺这一行时 ninja �? `no work to do`、宏不更新，�? VERSION 等于不生�?**）；`src/driver/driver.cpp` �? `Driver::printVersion()` 改用 CMake �? `target_compile_definitions` 注入的编译期�? `VB6C3_VERSION_STRING`（含 `#ifndef` 兜底）。README 第九章「版本号约定」同步改写（同步清单 �? 派生清单）。验证：�? `cmake -P` 单测读取逻辑（RAW ` 0.10.4\r\n` �? STRIP `0.10.4`）；�? 端到端传播实�? —�? VERSION 临时�? 9.99.9 �? 构建（自�? Re-running CMake + 重编�? driver）→ `C3 --version` = 9.99.9；恢�? 0.10.4 �? 构建 �? = 0.10.4，VERSION 字节�? git 状态干净；③ 回归 82/0/1/83
- 2026-09-17（发现，未修）：`C3 --version` �? `-h/--help` �?**退出码�? 1**，不�? 0 —�? driver.cpp 返回 `{opts, 0}`（注释�?0 = 显示帮助后退出」），main.cpp �? `runCompile` �? `!result.success && result.errorCount == 0` �? `return 1`。脚�?/CI �? `c3 --version` 会被判失败。是否改�? 0 待定（属既有行为，非本次引入�?

### 0.10.3

- 2026-09-17 源码拆分（函数体片段推广�?600+ 行口径）�?4 个单巨函数文件一次拆净 —�? cgen_expr_ident 744→主 26 + detail/ 3 片段�?224/190/323）、cgen_util 699→主 26 + 4 片段�?64/328/229/76）、cgen_setlet 809→主 264 + 2 片段�?313/247）、cgen_assign 1367→主 28 + 4 片段�?397/426/230/310）；切点全部取函数体内既有的顶层语义分节注释（零重排），每例断言「head + 片段 + tail 拼回原文件逐行一致」；13 �? .inc 不需登记 CMakeLists（不是编译单元）；全量构建增量通过。src �? �?500 行文�? 12 �? 8
- 2026-09-17 回归运行器修复（tests/run_tests.ps1）：新增 `Invoke-TestExe` 统一启动出口 —�? 路径 A�?.NET `ProcessStartInfo` + `ReadToEndAsync` 管道捕获）优先、路�? B（`Start-Process -Redirect*`）后备，两条都失败才�? FAIL �?**打印异常原文**（旧实现 catch 只写 `FAIL (run error)`，把环境问题伪装成测试失败）；`Test-Run` / `Test-Vbp` 两处重复逻辑收敛到该函数。`.out`/`.err` 仍按原样落盘供人工复查；不改测试语义、不�? CMake。修复后 `test smoke` PASS、全�? `test all` 回到基线 `PASS=82 FAIL=0 SKIP=1 TOTAL=83`
- 2026-09-17（修正上一版归因）：把「FAIL=39 全假阴性」归因为「Start-Process 被会话安全策略阻断�?**不成�?** —�? 复测 `Start-Process -NoNewWindow -Wait -RedirectStandardOutput` 本机可用，HEAD 版原文在相同调用方式（bat �? `powershell -File`，含 bash 管道）下 3/3 通过�?**无法稳定复现**；当时唯一可复现的缺陷是运行器吞异常。事件特征留档：39 �? FAIL 恰等于全部可运行 run 用例数，`.out`/`.err` 存在�? 0 字节（进程没起来�?
- 2026-09-17（环境注记，仍然有效）：`reg.exe` 被程序黑名单拦截 �? vcvarsall 拿不�? Windows SDK 路径（`C1083 crtdbg.h` / `winsock2.h`），规避办法是把 `C3_VCVARSALL` 指向�? set `INCLUDE`/`LIB`/`PATH` 的替�? bat
- 2026-09-17 源码拆分（函数体片段 + 类体片段）：cgen_expr_member 1496→主 36 + detail/ 8 片段（precheck 119 / form_builtin 231 / obj_dispatch 190 / class_module 222 / m22_module 169 / generic_access 120 / voidptr_com 191 / class_fallback 239），首次在「相对花括号深度 1」处切分（原 173~773 行是一�? `if(IdentifierExpr)` 巨块，深�? 0 只有 2 个点），片段自身不闭合但拼接后逐行一致；ast_printer 581→主 47 + ast/detail/ 5 个类体片段（entry 13 / decl 93 / stmt 222 / expr 93 / support 128），�? `class PrintVisitor` 类体�? `#include`（继 cgen.hpp 之后第二次用类体片段）。拆前存 `--dump-ast` 基线�?18 样例 / 464 行），拆后逐字节一�? 18/18；增量构建零错误；回�? 82/0/1/83。src �? �?500 行文�? 8 �? 6
- 2026-09-17（环境注记，x86 路径）：`--arch x86` �? C3 内部�? `call vcvarsall.bat x86`（msvc_driver_discovery.cpp �? buildVcvarsPrefix），�? reg.exe 被拦 �? `C1083 windows.h`，表现为回归里恰�? 2 �? `FAIL (compile)`（test_earlybound2 / test_not_com，run_tests.ps1 里唯一�? -Arch x86 用例）—�? 不是代码回归。规避：�? `VCINSTALLDIR` 指向�? VS 目录 `%TEMP%\c3_fake_vc`，其下放�? %1 �? x64/x86 �? `Auxiliary\Build\vcvarsall.bat` 替身；配 `C3_VCVARSALL=%TEMP%\wb_vcvarsall_fake.bat` 后回归回�? 82/0/1/83

### 0.10.2

- 2026-09-17 源码拆分（生成物）：vb6_di_win32_stubs.c 1184→di/ �? 7 个按 Lib 家族生成的桩文件（最�? 377 行）+ 手写 vb6_di_stubs.c 204；先�? cgen_decl_api.cpp �? vb6_di_lib 标记，再�? gen_di_stubs.ps1 摆脱 unresolved_syms.txt 并支持重跑；RTL 管线 41�?47，重�? 99/99 + e2e 2/2 + 回归 82/0/1/83�?
- 2026-09-17（版本）：三处版本号统一�? 0.10.2 —�? VERSION / driver.cpp �? `C3 version` / CMakeLists.txt �? `project(... VERSION ...)`（此�? 0.10.1 / 0.10.0 / 0.1.0 三处不一致）
- 2026-09-17 源码拆分（纯搬移）：semantic_analyzer.cpp 666→主 317 + dispatch 95 + register 177 + typeref 114，分派辅�? / 注册声明（Pass1�?/ 类型引用解析各成一家；保留�? + 三区间拼回与原文件一致（仅丢 2 处多余空行）；重�? 99/99 + e2e 2/2 + 回归 82/0/1/83。src �? �?500 行文�? 14 �? 13
- 2026-09-17 源码拆分（函数级抽取首例）：semantic_analyzer_builtin.cpp 795（registerBuiltins 单函�? 780）→ 26 行伞文件 + src/semantics/builtin/ 3 个「函数体片段」（consts 289 / consts_ext 211 / funcs 295），片段在函数体�? #include、局�? lambda �? kVariantArray 原样不动 �? 零重构零行为改动；三片段拼回与原文件逐行一致；重建 96/96 + e2e 2/2 + 回归 82/0/1/83。src �? �?500 行文�? 15 �? 14
- 2026-09-17 源码拆分（RTL）：vb6forms_picture.c 528�?275+274、vb6forms_widget.c 502�?231+291，主文件逐字节不动；RTL 5 处管线全同步（rc 139/140 + 枚举 + files[] + driver sourceFiles + CMake），四处清单计数核对 41 一致；重建 96/96 + e2e 2/2 + 回归 82/0/1/83。src �? �?500 行文�? 17 �? 15
- 2026-09-17 源码拆分（续）：再拆 5 个「单函数主导但主导函�? <500 行」的文件 —�? cgen_com 682�?257+439、parser_decl 655�?274+390、parser_expr 590�?402+199、typelib_builder 527�?345+203、frm_parser 516�?320+209；主文件逐字节不动，5 个新文件登记 CMakeLists；重�? 96/96 + 回归 82/0/1/83 零变化。src �? �?500 行文�? 26 �? 17
- 2026-09-17 源码拆分（② 组）�?4 个「单函数主导但主导函�? <500 行」的文件纯搬移拆净 —�? cgen_localdecl 552�?375+191、cgen_decl_proc 552�?256+309、semantic_analyzer_decl 551�?387+178、cgen_expr_binary 514�?356+172；主文件逐字节不动，4 个新文件登记 CMakeLists；重�? 91/91 + 回归 82/0/1/83 零变化。同时修正台账口径：单函数主�? �? 必须函数级抽�?
- 2026-09-17 cgen 头拆分：cgen.hpp�?996 行单类头）拆为伞�? 54 �? + cgen_emitter.hpp（CodeEmitter 独立成头�?+ detail/ 3 个类体片段（api 203 / state 342 / helpers 382），类内 #include 片段、逐行零重排，36 处引用路径不变；回归 82/0/1/83 零变�?
- 2026-09-17 AST 头拆分：ast.hpp�?1459 行）拆为伞头 18 �? + src/ast/detail/ 8 子头（enums/fwd/base/expr/stmt/stmt_io/decl/util，最�? 366 行），按类边界切、零重排�?6 处引用路径不变；回归 82/0/1/83 零变�?
- 2026-09-17（ai/003-开发计划）：补「实现说明」——P4~P8 �? rtl/*.hpp/cpp 规划名是早期 C++ 路线，实际走 C �? RTL，相关文件名已不存在；原计划文本不改�?
- 2026-09-17（src/rtl/core）：41 个平铺文件按家族�? 4 个子目录（vb6rtl 9 / vb6com 8 / vb6comserver 7 / vb6forms 13），根下只留 2 �? DI �? + bstring；同�? rc / CMake 路径，CMake 嵌入依赖�? 17 项补齐为 39 �?
- 2026-09-17 RTL 头拆分：vb6rtl.h�?1007 行）拆为伞头 16 �? + 7 子头（base/bstr/variant/builtin/array/class_com/runtime），同步 4 处嵌入管线，回归 82/0/1/83 零变�?

###  0.10.1
- 2026-09-17 清理：删 20 个纯占位空文件；同步 ai 文档旧路径；更新外壳 README / CLAUDE
- 2026-09-16 源码拆分专题：巨型文件按家族拆到�? 500 �?/文件（RTL 3 �? + C++ 6 个，另含 backend 目录重组�?
- 2026-09-16 Fix 099（parser_expr.cpp）：归因过程中最大反转——诊断打�? 0 命中证明问题根本不在 cgen，而在 parser：With 块内 Users.Decode .Rs 被误解析成链式成员访问而非“实�? .Rs”。Fix 077 的空格检测扩展到 obj.Method .Field 形态�?-2
- 2026-09-16 Fix 098（cgen_stmt.cpp）：Set 左值是函数调用表达�? �? 求值后丢弃（VB6 语义 no-op）�?-2
- 2026-09-16 Fix 097（type_system.cpp + cgen_expr.cpp）：CompareMethod（VBA 枚举的无 vb 前缀别名）两处统一映射 Long�?-1
- 2026-09-16 Fix 096（cgen_com.cpp）：WithEvents 未实现的事件生成�? stub，vtable 不再悬空引用�?-3
- 2026-09-16 Fix 095（driver.cpp）：撞名时标准模块过程优先于类成员——mWebSocketUtils.bas �? cWebSocketUtils.cls 双定义，裸调该解析到模块版（�? me 参数）�?-13，一处修复清�? WebSocket 全家
- 2026-09-14~15 工程化：脚本 vswhere + 环境变量可移植；顶层目录英文化；README / CONTRIBUTING / ENV；冒烟测试；VERSION
- 2026-09-11 M31-M33：链接期 LNK1104 / LNK2019 / LNK2005 全部清零，VBMAN.dll 可注册（tag v0.1.0-m33-vbman-dll�?
- 2026-09-02 编译提速：/MP 并行 + --no-warn + --incremental + --trim-includes
- 2026-09-01~10 vbman 编译期错误清零：777 �? 0，全�? bisect 子集跟踪
- 2026-08-31 x64：LongPtr / LongLong 类型 + 指针安全运行�?
- 2026-07-18 M29/M30：COM DLL DISPID 不一致（Calc.Add 找不到成员）；x86 交叉编译路径拼接
- 2026-07-14 M27/M28：第三方 COM 事件 SourceIID；VB6 字符串语义（vbCrLf / 双引号转义）+ Format �? 600 行重�?
- 2026-07-06~10 P24/P25：COM 后期绑定 + 集合枚举 + 服务质量；frxParse 端到端跑�?
- 2026-07-02~05 M12/M13：FRX 二进制资源读取；双架�? x64/x86；COM ProgID / Variant 数组索引 / 定长字符串系列修�?
- 2026-07-01 M22：首个真�? VB6 工程（realVBP + 窗体 demo）编译链接成功，9 项运行时修复
- 2026-06-30 P17-P21：兼容性填平（Batch A-F�?+ 74/74 回归零失败基线，M14
- 2026-06-29 P14-P16：兼容性深�? + WithEvents 控件事件 + ActiveX DLL 能力验证，M11/M12
- 2026-06-27 P8-P10：TypeLib 内建生成替代 MIDL；RTL 改以 RC 资源内嵌（零配置编译），M9/M10
- 2026-06-26 P5-P7：多模块工程 / COM 对象模型 / 窗体控件三项综合验证，M6-M8
- 2026-06-25 P2-P4：两遍语�? + 120+ 内置符号；C 代码生成 + MSVC 驱动，编译出 hello.exe，M3-M5
- 2026-06-24 P0-P1：词法器（LL(2)+Unicode�?+ 解析器（�? 2900 行）跑通，M1/M2

## 2026-09-22 Fix 194: COM ������ VARIANT ABI ������ demo ���� (0xC0000374)
- ����: VBMAN demo ������ 0xC0000374 ����, ��Ъ��, CI �뱾��ͬǩ����
- ��λ: RTL VEH ץ 0xC0000374 ��ջ + IFEO ҳ�Ѱ��𻵱�� AV, ҳ�������� VBMAN.dll �� memcpy 24B дԽ��, ������ Form_Load->ComGetProp->ComObj_Invoke��
- ����: �������������� vb6_VARIANT (x86 24B) ���ֶ�д invokeFunc ��ʵ��/���ز�, �� COM �߽� OLE VARIANT �� 16B, ͸��/ֱдԽ�� 8B��
- �޸�: ComObj_Invoke ʵ�θ��� 24B ����ת (������ 16B OLE �����ٰ���ǿת), ���ز� 24B ��ת���д 16B; ����ѭ�����ٶԲ�λ VariantClear (�����ǵ��÷���Դ����)��
- ����: vb6forms.c VEH ֧�� 0xC0000005 �ֳ� (��д��־/�Ĵ���/ջɨ�辵�񷵻ص�ַ), C3_CRASH_TRACE=1 ʱ��Ч��
- ��֤: ���� x86 ȫ��· 3/3 �ޱ��������̽�봰�ڽ�����

## 2026-09-22 Fix 195: VEH ջɨ��/�Ĵ�����ӡ���� x86 ר�� CONTEXT �ֶ�, x64 �಻��
- ����: Fix 194 �� VEH (vb6forms.c) ������ʹ�� CONTEXT::Esp/Eip/Eax �� x86 �ֶ�, �� RTL Ƕ���������� exe, x64 Ŀ��ȫ�� C2039 (�ع� FAIL=62 ������֮һ)��
- �޸�: ջɨ�谴 _WIN64 �� Rsp/ULONG_PTR (QWORD ֵ), �Ĵ�����ӡ x64 �� Rip/Rsp/Rax ϵ��
- ������ע: ����ɳ��������� reg.exe, C3 �� vcvarsall ǰ׺ʱ��·�� -> cl �� INCLUDE �� C1083, ������޹�; CI ����Ӱ�졣
- ��֤: x64 hello.bas ����+����ͨ�� (Sum=5050)��

## 2026-09-22 Fix 196: Is/�Ƚ�ȡַ�㿽 VARIANT ��ʱ, vbman ���� SSE/Heartbeat ģ�鴥�� C2440
- ����: vbman �������� SSE ȫ��Ͱ�� Winsock/cHeartbeat �� CI ���� C2440 ����: cHeartbeat.c(151) int32_t(vb6_ComGetIntProp)->vb6_VARIANT, cSSE.c(166) vb6_cls_cClientCallback*->vb6_VARIANT��
- ����: cgen_expr_binary.cpp variantAddr158n �㿽��ֻ֧�Ժ� -> �ı���ʽ�� vb6_VariantFromValue (Fix 158u), ���� rvalue (���ͻ� getter ���� int32_t / ��Ŀ��ָ�������) ֱ�� vb6_VARIANT tmp = X �㿽��
- �޸�: �㿽��֧ͳһ�� vb6_VariantFromValue (_Generic: ���� VARIANT ���ֱ��, ����/ָ���Զ���װ); �� vb6_ComCall( ������� VariantFromComResult ������ (Fix 132 ͬ����)��
- ��֤: ���� x86 ȫ�� 129 ģ����� exit=0, VBMAN.dll ���� (2.3MB)��

## 2026-09-22 Fix 197: ��ģ�� Public ö�ٳ�ԱδԤע��, Optional Ĭ��ֵ���� 0
- ����: vbman HTTP ��������� accept ���� (CI demo CLOSE_WAIT + HTTP ��ʱ)������̽��ʵ֤ cTlsSocket.Create �յ� EventMask=0 (ȱʡֵ ucsSfdAll=63 δ��Ч) -> WSAAsyncSelect(lEvent=0) ����ע��֪ͨ -> ��Ϣ�����ɷ���
- ����: evalOptionalDefault ����ű������� Pass 1 �ڼ�, �� runCrossModuleResolution ������ע���ģ�����; Fix 047 �� Pass1 ǰԤע��ֻע�� EnumType ������, δע�� EnumMember ���� -> ����ö��Ĭ��ֵ lookup ʧ�ܻ��� 0��ͬģ����ö��Ĭ��ֵ����Ӱ�� (��С���� optenum4 ʵ֤, �޸�ǰ 0 �޸��� 63)��
- �޸�: driver_semantics.cpp Fix 047 Ԥע�����չ �� ����ģ�� Public Enum �ĳ�Աһ��Ԥע�� (evalEnumConstIntForDriver ��ֵ, ���� Fix 191 ��ֵ��, �� VB6 ��������)��
- �޸��󱾻�ʵ��: mask=0 -> mask=63, WSAAsyncSelect ע��ɹ�; demo �ɡ����� accept��ǰ���������Ӻ� 0xC0000005 ������, �ϵ�ת���� cAsyncSocket wndproc ������ thunk (InitAsyncSelectNotifyThunk) �� c3 ��Ŀ����󲼾�ʧ�� (EXECUTE(DEP) target=0), ������������
